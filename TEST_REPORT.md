# TNR 流浪动物管理系统 · 测试报告与优化清单

> 首轮测试日期：2026-09-13 · 代码基线：`cnb.cool/tnrxy/TNR` main `b6d9c59`（添加部署文档）
> 二轮专项日期：2026-09-16 · 范围：捕捉端 / 捕捉管理业务逻辑全量走查
> 三轮专项日期：2026-09-16 · 范围：其余全部模块横向走查 + 种子/部署数据一致性
> 四轮专项日期：2026-09-16 · 范围：部署路径与安全默认值（控制合理性）
> 五轮专项日期：2026-09-16 · 范围：前后端契约一致性 + 区县归属（数据可见性）
> 六轮专项日期：2026-09-16 · 范围：前端 GUI 浏览器实测（真实渲染层）
> 七轮收尾日期：2026-09-16 · 范围：测试数据清理 + 区县名笔误更正
> 八轮专项日期：2026-09-16 · 范围：权限矩阵穷举 + 区县隔离 FieldError
> 九轮专项日期：2026-09-16 · 范围：捕捉端 10 项需求改造 + 政府端一宠一档
> 十轮专项日期：2026-09-18 · 范围：台账详情 / 看板 / 转运交接 / 驳回回池
> 十一轮专项日期：2026-09-18 · 范围：列表冻结 + 去汇总标题
> 十二轮专项日期：2026-09-18 · 范围：全列表搜索条件 / 分页 / 去标题 / 高度下限 10 行
> 测试环境：macOS / Python 3.12.12 / Django 5.0.6 / SQLite（开发库）

---

## 一、测试概览

| 项 | 结果 |
|---|---|
| 全新自动化测试套件 | **769 个用例，全部通过**（约 58 秒，不依赖 seed_data） |
| 旧测试套件（参考基线） | 36 个用例，通过后作为契约参考，已被新套件取代 |
| 浏览器 GUI 黑盒走查 | 四端核心流程全部走通；六轮补齐真实渲染层实测（捕捉端 31 项 + 四端巡检 12 项）；九轮再验 10 项需求改造；十二轮逐页逐标签审计 **80 个视图 0 报错 0 空白** |
| 真实 HTTP 冒烟测试 | **72 项检查全部通过**（二轮 37 项 + 三轮 35 项，见第 2.7 / 2.8 节）+ 五轮端到端可见性验证 + 八轮权限矩阵穷举 |
| 发现并修复的真实缺陷 | 首轮 17 项 + 二轮 14 项 + 三轮 13 项 + 四轮 8 项 + 五轮 8 项 + 六轮 1 项 + 八轮 1 项 + 九轮 6 项 + 十轮 6 项 + 十二轮 3 项 + 十五轮 1 项越权 + 十六轮 4 项越权/越界 + 十七轮 3 项越权/越界 + 十八轮 3 项控制失效 + 十九轮 1 项权限/契约不一致 + 二十轮 3 项横向越权 |
| 测试数据清理 | 测试痕迹 **41 条记录 + 5 个媒体文件**已清除，演示数据完整保留（见 2.12） |

新测试套件结构（替代原单文件 `business/tests.py`）：

```
business/tests/
├── base.py                    # 最小化夹具工厂 + API 助手（自建数据，结果确定）
├── test_services.py           # 编号生成/芯片/库存/黑名单/区县隔离/序列化
├── test_models.py             # 模型约束、默认值、PROTECT/级联
├── test_capture_views.py      # 捕捉登记/编号预览/主人领回/合照上传
├── test_transfer_views.py     # 转运双格式创建/拆分防重/签收/驳回/重发
├── test_treatment_views.py    # 诊疗四项/库存扣减/芯片绑定/状态机
├── test_material_views.py     # 采购/下发/签收/异动/双台账/芯片号段
├── test_release_views.py      # 放养创建/小区匹配/确认闭环
├── test_adoption_views.py     # 公开大厅/线下登记/在线申请/审核/信息编辑
├── test_checkin_views.py      # 回访打卡/重复拦截/审核状态机
├── test_blacklist_views.py    # 黑名单增查/模糊匹配不误伤
├── test_euthanasia_views.py   # 安乐死登记/遗体领取
├── test_portal_views.py       # 门户渲染/消息/宠物全生命周期溯源
├── test_tasks.py              # 诊疗完成 5 天自动转待领养
├── test_seed_data.py          # 种子数据幂等性 / 演示账号自愈 / 芯片号段一致性
├── test_migrations.py         # 数据迁移 0010/0011：区县改判、机构改名、北京地名清理（含幂等）
└── test_integration.py        # 捕捉→转运→签收→诊疗→放养/领养 全链路
accounts/tests.py              # 登录/角色重定向/api_me/用户模型/修改密码/ensure_superuser
core/tests.py                  # 区县/机构模型 + 安全配置不变量与生产缺配置即失败
core/tests_frontend_consistency.py  # 前后端契约：枚举映射覆盖 / 禁止状态硬编码 / 路由对齐 / 区县默认值
supervision/tests.py           # 政府端权限边界/大屏/机构/区县/用户/配置
```

覆盖要点：5 角色 RBAC 权限矩阵（401/403/302 分支）、区县数据隔离、状态机越权拦截、
医院库存联动扣减、芯片唯一不可复用、黑名单「前 6+后 4」匹配不误伤、重复提交拦截、
数量边界（>100 拒绝）、库存不为负、全生命周期事件链。

运行方式：`python manage.py test --parallel 1`

---

## 二、发现并修复的缺陷（12 项）

### 功能缺陷（测试驱动修复）

| # | 缺陷 | 修复 | 提交 |
|---|---|---|---|
| 1 | `decorators.py` 中 `redirect('login')` 的 URL name 不存在，匿名访问受角色保护的页面直接 **500** | 改为 `redirect(settings.LOGIN_URL)` | f61d25e |
| 2 | API 视图 `@login_required` 叠在 `@role_required` 外层，匿名调用 API 返回 **302 跳登录页 HTML**，前端无法识别 401 | 统一交换 68 处装饰器顺序，匿名 API 一律返回 401 JSON | f61d25e |
| 3 | 捕捉登记/领养信息编辑声明支持 multipart 照片上传，但表单字段从不读取 `request.POST`，multipart 提交必然报错 | 补充 `request.POST` 回退解析（与打卡接口同款兼容写法） | f61d25e |
| 4 | 宠物全生命周期溯源把 `created_at`（datetime）与 `released_at`（date）的 ISO 串混排，**同日事件排序错乱**（放养排到捕捉前面） | 事件日期统一归一化为本地日期 | f61d25e |

### 走查发现的体验缺陷

| # | 问题 | 修复 | 提交 |
|---|---|---|---|
| 5 | 捕捉点端 8+ 处时间列显示原始 UTC ISO 串（`2026-09-13T09:15:58.575378+00:00`） | 统一走 `TNR_UI.formatDateTime` 本地化格式 | 4a0355c |
| 6 | 表格操作列「详情/编辑/删除」列宽不足，**逐字竖排折行** | CSS：操作列与链接按钮 `white-space: nowrap` | 4a0355c |
| 7 | 转运单创建成功提示冒号后为空（读取了不存在的 `data.ledger_no`，实际 data 是数组） | 兼容数组取 `ledgerNo`，多单逐一列出 | 4a0355c |
| 8 | 捕捉点端转运「已下发」标签页对应后端不存在的状态，**永远为空**；「待下发」文案与后端「待签收」语义相悖 | 移除死标签页，文案统一为「待签收」 | 4a0355c |
| 9 | 医院端诊疗基础信息空值渲染成孤立的「/」（如「性别/年龄：/」） | 无值显示「—」，有值才拼接「/」 | 4a0355c |
| 10 | 首页看板空待办显示突兀的「正常」徽标 | 空态去掉徽标，改为友好文案 | 4a0355c |
| 11 | 公开领养大厅首屏**空数据**（种子数据只有待领养宠物、无上架信息），给访客的第一印象是空白页 | 种子数据新增领养大厅上架记录（黑犬三号，含简介/流程） | 4a0355c |
| 12 | 政府大屏残留无效死代码行（对空数组 forEach）；「捕捉总量」口径（动物只数）与后端 stats API 口径（登记记录数）不一致且无标注 | 删除死代码；卡片标注「累计捕捉（只）」 | 4a0355c |

---

## 二点五、用户实测反馈修复（2026-09-13 晚）

用户以 admin 实测时触发自锁：在账号管理中停用了自己的账号（日志
`POST /api/supervision/users/1/toggle/`），此后无法登录，且登录页仅提示
「用户名或密码错误」，误导排查方向。修复（提交 `2c4d488`）：

| # | 问题 | 修复 | 测试 |
|---|---|---|---|
| 13 | 管理员可在账号管理中**停用自己**，停用即被锁死 | 后端禁止停用当前登录账号自己 | test_toggle_self_rejected |
| 14 | 最后一个启用中的市级管理员可被停用，系统失去账号管理入口 | 停用前校验，至少保留一个启用中的市级管理员 | test_district_admin_cannot_deactivate_last_city_admin |
| 15 | 超级管理员可被停用 | 超级管理员不可停用 | test_toggle_superuser_rejected |
| 16 | 停用账号登录时提示「用户名或密码错误」，误导排查 | 明确提示「该账号已被停用，请联系管理员恢复」 | test_login_inactive_account_clear_message |
| 17 | 政府端账号列表对自己的行也显示「停用」按钮，易误点 | 当前登录账号行显示「当前账号」标记，不再提供按钮 | 前端改动 + 后端兜底 |

同时验证了用户关心的**区县改名联动**：系统中区县引用全部走外键实时读取
（无名称快照），改名后账号列表、机构列表、业务数据、大屏统计等处自动展示
新名称，已用测试 `test_district_rename_propagates` 与线上 API 实测双重验证。
种子数据默认使用北京区县（朝阳/海淀/西城/东城 + 全市市级）；用户手工改名的
数据会保留，需要恢复北京演示数据可执行 `python manage.py seed_data --flush`。

---

## 二点七、捕捉端 / 捕捉管理专项修复（2026-09-16）

用户实测反馈的 7 项缺陷，逐项定位根因并修复，并按同一思路对捕捉端全链路做了
横向排查（连带发现并修复 7 项同类问题）。

### 2.7.1 用户报告的 7 项缺陷

| # | 用户描述 | 根因 | 修复 | 验证用例 |
|---|---|---|---|---|
| 1 | 新增录入表单的定位地址没有作为默认地址、提交后也没入库 | 前端定位结果只更新提示文案，未写入地址输入框；后端只读 `address`，`geo_address` 为空时不做兜底 | 前端 `applyLocation()` 把定位地址填入「详细地址」并新增 `addrEdited` 标志（人工改写过就不再被覆盖）；后端 `address = data['address'] or geo_address` | `test_geo_address_fallback`、冒烟 §2 |
| 2 | 小区搜索是下拉枚举，应为输入文字后提交搜索 | 前端筛选栏把小区配成 `type:'select'` 并枚举已有小区；后端按精确匹配过滤 | 前端改为 `type:'search'`（回车搜索）+ 大小写不敏感「包含」匹配；后端改 `community_name__icontains` | `CaptureListSearchTest`、冒烟 §4 |
| 3 | 列表详情缺少新增表单里的内容（地址、照片、签名等） | 详情面板只渲染列表接口返回的字段，而列表接口本身缺字段且无宠物/签名 | `capture_detail` 返回全字段 + 宠物列表 + 签名；详情面板补齐整体合影、单只照片、电子签名、经纬度、定位地址、转运情况 | `CaptureDetailTest`、冒烟 §3 |
| 4 | 编辑改不了主要信息，小区仍是下拉；已转运的不应可改 | 编辑按钮是 `TODO` 占位（`TOAST('待后端接口支持')`），后端根本没有更新接口 | 新增 `POST /captures/<pk>/update/`；小区改为文本 `input#ed_communityName`；已转运（`can_edit=False`）时前端拦截 + 后端拒绝（403 语义 400 文案） | `CaptureUpdateTest`、冒烟 §5/§6 |
| 5 | 取消按钮不生效 | `tnr-common.js` 的 `modal()` 用 `querySelector('[data-action="close"]')`，**只绑定了页头的 `×`**；页脚「取消」按钮 data-action 相同但从未绑定事件，点击无反应 | 改为 `querySelectorAll(...).forEach(...)` 逐个绑定；`confirm()` 同样问题一并修复；另补 Esc 关闭与监听器清理 | 页面脚本静态校验 + GUI 走查 |
| 6 | 状态不能全是「已完成」 | `capture_create` 硬编码 `status='completed'`，模型默认值也是 `completed`，与真实转运情况无关 | 改为**派生状态机**：`pending 待转运 / partial 部分转运 / completed 已完成 / void 已作废`，由 `recalc_capture_status()` 依据转运单推导；迁移 `0007` 用 `RunPython` 回填历史数据 | `CaptureStatusTest`、冒烟 §3/§9 |
| 7 | 删除按钮不起作用；有转运的不能删，需医院全部退回才能删，且只能逻辑删除 | 同 #4，删除按钮也是 `TODO` 占位，无后端接口 | 新增 `POST /captures/<pk>/delete/`：`can_delete=False`（本单存在 `pending/received` 转运单）时拒绝并给出「需医院全部退回」文案；执行**逻辑删除**（`is_deleted/deleted_at/deleted_by` + `status='void'`），并级联逻辑作废其下宠物档案，数据库行保留 | `CaptureDeleteTest`、冒烟 §10 |

**状态机语义**（`business/services.py`）：

```python
ACTIVE_TRANSFER_STATUSES = ('pending', 'received')   # 已下发、尚未退回

capture_transfer_state(capture)
  → {total, transferred, rejected, untouched, can_edit, can_delete}
  can_edit = can_delete = (未删除 and transferred == 0)

recalc_capture_status(capture)
  已删除              → void      已作废
  无宠物 / 无有效转运单 → pending   待转运
  部分宠物已转运       → partial   部分转运
  全部宠物已转运       → completed 已完成
```

历史数据回填效果（迁移 `0007`，7 条旧记录）：全部 `completed` → `4 pending / 1 partial / 2 completed`。

### 2.7.2 横向排查连带发现并修复的问题（7 项）

| # | 问题 | 影响 | 修复 |
|---|---|---|---|
| 1 | **转运退回后宠物未解除医院归属** — `transfer_reject` 只把状态改回 `in_transit`，`pet.hospital` 仍指向医院 | 宠物既无法被主人领回，捕捉单也**永久锁死**不可编辑/删除（直接导致 #7 的删除条件永远不满足） | 退回时 `Pet.objects.filter(...).update(status='in_transit', hospital=None)` |
| 2 | 捕捉列表接口回传巨量 base64 签名 | 单条签名可达数百 KB，列表页响应臃肿 | `CAPTURE_LIST_EXCLUDE = ('signature',)`，仅详情接口返回 |
| 3 | 多字段 OR 搜索产生**重复行**；状态逐条计算造成 N+1 查询 | 列表出现重复记录、性能随数据量线性劣化 | 改用单条 `Q(...)` OR 组合；新增 `capture_states_bulk()` 批量推导 |
| 4 | 黑名单「编辑 / 移出」按钮点了没反应（与 #5 同一类 bug） | 黑名单只能新增不能维护 | 新增 `blacklist_update` / `blacklist_delete`（逻辑删除，字段 `is_deleted`/`deleted_at`）；`check_blacklist` 过滤已移出记录 |
| 5 | 个人中心「修改密码」无效 | 用户无法自助改密 | 新增 `POST /api/me/password/`：校验原密码 → `validate_password` → `set_password` → `update_session_auth_hash`（改密后不登出） |
| 6 | 监管端捕捉台账状态列**硬编码「已完成」** | 政府端看到的永远是「已完成」，与事实不符 | 改为按 `pending/partial/completed/void` 映射渲染；台账详情补物业名称/定位地址/经纬度/状态 |
| 7 | 逻辑删除的记录仍出现在监管统计、台账、医院宠物列表 | 已作废数据被重复统计，污染监管口径 | `supervision/views.py` 统计与台账、`business/views_portal.py` 医院宠物列表统一补 `is_deleted=False` |

### 2.7.3 新增接口

```
POST /api/business/captures/<pk>/update/     # 编辑（已转运则拒绝）
POST /api/business/captures/<pk>/delete/     # 逻辑删除（有在途转运则拒绝）
POST /api/business/blacklist/<pk>/update/    # 黑名单编辑
POST /api/business/blacklist/<pk>/delete/    # 黑名单移出（逻辑删除）
POST /api/me/password/                       # 修改密码
```

### 2.7.4 二轮验证结果

| 验证方式 | 结果 |
|---|---|
| Django 全量自动化测试 | 本轮新增用例后为 **422 个用例全部通过**（进入本轮时为 374 个基线） |
| `manage.py check` | `System check identified no issues (0 silenced).` |
| 数据库迁移 | `0007`（状态机字段 + 历史回填）、`0008`（黑名单逻辑删除）均已应用 |
| 真实 HTTP 冒烟测试 | **37 项检查，0 失败**，12 个分组覆盖登录→新增→状态推导→详情→搜索→编辑→锁定→退回解锁→逻辑删除→黑名单→改密 |
| JS 语法校验 | `node --check` 通过（`tnr-common.js` + 渲染后各端内联脚本） |

冒烟测试关键断言节选：

```
== 9. 医院全部退回 → 回到未转运 ==
  [OK] 医院驳回成功            → 已驳回
  [OK] 全部退回后状态回到待转运   → pending
  [OK] 退回后恢复可编辑、可删除
== 10. 逻辑删除 ==
  [OK] 删除成功                → 捕捉记录已删除（逻辑删除，含 2 条宠物档案）
  [OK] 已删除记录不再出现在列表
  [OK] 其下宠物档案已一并作废
  [OK] 重复删除被拦截           → 该捕捉记录已删除，请勿重复操作
  [OK] 已删除记录不可编辑
== 12. 修改密码 ==
  [OK] 改密成功                → 密码修改成功
  [OK] 改密后会话保持有效（未被登出）
  [OK] 错误原密码被拒绝         → 原密码错误
  [OK] 弱口令被密码校验器拒绝     → 密码长度太短。密码必须包含至少 8 个字符。；…
```

> 注：`123456` 是演示账号口令，Django 密码校验器会拒绝（太短/太常见/纯数字），
> 因此**无法通过改密接口恢复**，只能由 `manage.py shell` 执行 `set_password` 重置。
> 本次已确认全部演示账号口令仍为 `123456`。

### 2.7.5 环境注意事项

- 本机 `curl`/`urllib` 访问 `localhost` 会被沙箱代理拦截，脚本需 `ProxyHandler({})` 或 `no_proxy='*'`。
- 测试脚本不要命名为 `inspect.py`（会遮蔽标准库 `inspect`，Django 启动报循环导入）。
- 本次操作前已备份数据库：`db.sqlite3.bak-20260916-013355`。

> 注：本节最初记录「`hd_shelter`、`aixin_hosp` 为停用账号」——该状态已在第三轮
> 由 `seed_data` 的演示账号自愈逻辑修正，**9 个演示账号现已全部可用**（见 2.8.3）。

---

## 二点八、其余模块横向走查 + 数据一致性专项（2026-09-16 第三轮）

用户确认「按同一套思路与测试策略继续」后，对捕捉端之外的**全部模块**
（转运 / 诊疗 / 领养 / 安乐死 / 放养 / 物料 / 回访打卡 / 监管 / 门户）
做了同等深度的横向走查，并顺带核查了种子数据与部署路径的数据一致性。

### 2.8.1 横向走查发现并修复的缺陷（10 项）

| # | 模块 | 缺陷（根因） | 修复 |
|---|---|---|---|
| 1 | 诊疗 | **先落库后校验**：`Treatment` 先创建，再扣疫苗库存，之后才校验芯片；芯片非法时直接 return，留下孤儿诊疗记录且库存已扣 | 改为「先校验全部前置条件 → 再整体 `transaction.atomic()` 落库」，任一不通过零副作用 |
| 2 | 诊疗 | 数量解析写成 `vac.get('quantity', 1) or 1`，显式传入的 `0` 被悄悄变成 `1`，「数量必须大于 0」的校验形同虚设 | 去掉 `or 1`，并补 `int()` 转换与异常兜底 |
| 3 | 领养 | 「违规收回」前端只弹提示 + 写黑名单，后端**根本没有接口**（TODO）；动物仍「已领养」、领养记录仍「已完成」，收回原因丢失 | 新增 `adoption_reclaim` 接口，在**一个事务内**完成：撤销领养 + 动物置回待领养 + 重新上架 + 记入黑名单（含原因），并通知领养人 |
| 4 | 领养/定时 | **幽灵待领养**：领养完成时上架记录被置 `is_active=False`，5 天自动转待领养用 `get_or_create` 不会重新激活它 → 宠物是「待领养」却不出现在领养大厅 | `auto_promote_to_adoptable` 显式把已存在的上架记录置回 `is_active=True` 并同步医院 |
| 5 | 全局 | 多处裸写 `Model.objects.get(id=pk)`，**不做任何区县范围校验**——任意登录用户猜到主键即可跨区县读取/篡改他区数据（涉及领养/放养/诊疗/安乐死/物料/打卡/门户/转运/监管） | 新增 `get_scoped_object()` / `get_active_pet()` 统一助手，替换全部裸查；越权一律 404 |
| 6 | 全局 | 已逻辑删除的宠物仍可被领养大厅展示、被登记诊疗、被打卡 | 大厅列表过滤 `pet__is_deleted=False`，诊疗/打卡取宠物时排除已作废档案 |
| 7 | 领养/物料 | `qs.filter(a) \| qs.filter(b)` 链式 OR 会**丢掉前置过滤**并产生重复行——物料台账那处直接**击穿区县隔离**，泄漏他区流水 | 统一改为单个 `Q()` 组合表达式 |
| 8 | 物料 | 医院侧库存由流水累加得出，`adjust_stock` 对医院不做余额校验 → 异动可把库存扣成**负数** | `stock_adjustment` 自行校验 `get_hospital_stock` 余额 |
| 9 | 物料 | 下发可跨区县串货；`chip_numbers` 传逗号分隔字符串时被**逐字符迭代** | 增加「只能下发到本区县医院」校验；字符串入参按逗号切分 |
| 10 | 领养/放养 | **双重承诺**：同一只动物可同时处于「待放养」与「待领养」两条流程中 | 双向互斥校验 `pet_has_pending_release` / `pet_has_active_adoption` |

监管侧同时修掉一处**提权**：`user_toggle_status` 原本允许区级管理员停用**市级**
管理员账号（旧测试甚至把这个越权行为当成预期写进了断言）。现已按区县范围收敛，
并保留了「系统至少保留一个启用中的市级管理员」的兜底。

### 2.8.2 数据一致性专项：种子数据与部署路径（3 项）

`DEPLOY.md` / `start_dev.sh` / `DEMO_ACCOUNTS.md` 都把 `seed_data` 描述为**幂等**
命令，且 `deploy.sh` 每次部署都会执行它。实测这三条承诺当时都不成立：

| # | 缺陷（根因） | 现场后果 | 修复 |
|---|---|---|---|
| 11 | **机构去重键用了 `name`**，而机构名是可在界面上修改的展示字段 | 区县被改名后，每次部署都会再插一套同名机构——现场库已出现 2 个零引用的孤儿捕捉点（「朝阳区流浪动物捕捉点」「海淀区流浪动物捕捉点」） | 给 `Institution` 增加稳定业务编号 `code`（与 `District.code` 同套约定），种子数据改按 `code` 幂等；迁移 `core/0003` 回填历史编号 |
| 12 | **芯片号段补零位数写错**：`f'1000010{i:04d}'` 生成 11 位的 `10000100001`，而宠物档案 `Pet.chip_no` 与诊疗记录 `Treatment.chip_no` 存的是 10 位的 `1000010001` | 芯片库存与业务档案**对不上号**：给这几只宠物登记「芯片植入」时，接口按 `chip_no` 查不到芯片，直接报「芯片 1000010001 不存在」；现场 4 条宠物档案 + 3 条诊疗记录全部悬空 | 改为 `{i:03d}`；迁移 `business/0009` 把现场 500 条 11 位芯片号规整为 10 位 |
| 13 | `_seed_users` **只在 created 分支写属性**，账号被停用或改了归属后重跑修不回来 | 现场 `hd_shelter`、`aixin_hosp` 为 `is_active=False`，无法登录；而部署流程看起来是「成功」的 | 每次执行都校准角色/电话/区县/机构/姓名/`is_active`，并打印被校准账号清单；**密码仍只在新建时写入**（静默覆盖生产环境改过的凭据是危险的，改密按文档手工流程） |

同时清理了现场库中的脏数据（均已逐个做引用审计）：

- 删除零引用孤儿机构：`朝阳区流浪动物捕捉点(#11)`、`海淀区流浪动物捕捉点(#12)`
- 删除自闭环测试残留：区县 `语法检查区(#6)` + 机构 `语法检查捕捉点(#13)` + 用户 `syntax_check_user`
- 恢复被停用的演示账号 `hd_shelter`、`aixin_hosp`

种子数据里残留的北京地名（区县/机构/小区/地址/身份证前 6 位）也一并改为与项目
实际（襄阳市）一致，避免出现「北京区县名 + 襄阳业务数据」的错配。

### 2.8.3 三轮验证结果

| 验证方式 | 结果 |
|---|---|
| Django 全量自动化测试 | **434 个用例全部通过**（`Ran 434 tests in 34.204s / OK`），其中新增 12 个种子数据用例 |
| `manage.py check` | `System check identified no issues (0 silenced).` |
| 数据库迁移 | `core/0003`（机构编号 + 回填）、`business/0009`（芯片号段规整）均已应用 |
| 真实 HTTP 冒烟测试 | 功能与越权拦截 **26 项检查，0 失败**；芯片流程专项 **9 项检查，0 失败** |
| 种子幂等性现场验证 | 连续两次执行 `seed_data`，17 个模型计数**全部零变化** |
| 演示账号可用性 | 9 个账号 `is_active` + `status=active` + 口令 `123456` 全部通过 |
| JS 语法校验 | `node --check` 通过（`static/js/tnr-common.js`、`static/js/tnr-api.js`、`js/tnr-common.js`） |

冒烟测试关键断言节选：

```
== 2. 违规收回（原先前端只弹提示、后端没有接口）==
  [OK] 违规收回成功              -> 已收回动物、撤销领养并记入黑名单
  [OK] 领养记录状态 -> 已取消      -> cancelled
  [OK] 动物状态 -> 待领养         -> pending_adopt
  [OK] 已下架的上架记录被重新激活   -> True
  [OK] 黑名单归属正确区县          -> 2
== 4. 跨区县越权拦截（cy_gov 在 襄城区，目标在 樊城区）==
  [OK] 跨区县违规收回 -> 404      -> 领养记录不存在或无权访问
  [OK] 跨区县读生命周期 -> 404     -> 宠物不存在或无权访问
  [OK] 跨区县建放养单 -> 404       -> 宠物不存在或无权访问
  [OK] 跨区县物料异动 -> 404       -> 物料不存在或无权访问
  [OK] 跨区县读诊疗记录 -> 404     -> 诊疗记录不存在或无权访问
== 5. 正向对照（同区县必须放行，避免越权校验过紧）==
  [OK] 本区县建放养单放行          -> 放养记录创建成功，待小区确认
  [OK] 领养大厅不出现已作废宠物     -> 共 2 条，含已删除=False
== 芯片流程专项 ==
  [OK] 已用芯片 1000010011 在芯片库存中存在   -> 修复前应为 False
  [OK] 已用芯片 -> 提示「已被使用」而不是「不存在」 -> 400 芯片 1000010011 已被使用
  [OK] 真实不存在的芯片 -> 仍提示「不存在」      -> 芯片 9999999999 不存在
  [OK] 可用芯片登记成功                       -> 诊疗记录创建成功
  [OK] 宠物档案 chip_no 已回写                -> 1000010016 vs 1000010016
```

> 说明：「跨区县 → 404」而不是 403，是有意为之——403 会泄漏「该主键存在」
> 这一信息，攻击者可据此枚举出他区数据规模。统一返回 404 与「不存在」同款文案。

### 2.8.4 本轮操作前后备份

- 迁移与数据清理前备份：`db.sqlite3.bak-20260916-015615`
- 冒烟脚本通过 ORM 自建夹具并在结束时自清理，跑完现场库无残留（已断言）

---

## 二点九、部署与安全控制专项（2026-09-16 第四轮）

前三轮走的是业务逻辑，这一轮补上「控制合理」里的另一块：**部署路径与安全默认值**。
部署脚本和 settings 平时不参与业务，但它们决定了「出错时是安静地不安全，还是大声报错」。

### 2.9.1 部署脚本缺陷（4 项）

| # | 缺陷（根因） | 后果 | 修复 |
|---|---|---|---|
| 1 | **脚本提示的超级管理员凭据是错的**。脚本先跑 `seed_data`（它已建了一个**非超级用户**的 `admin`），之后才判断「若 `admin` 不存在则创建超级用户」——条件恒为假、分支永不执行，结尾却打印「超级管理员: admin / admin123456」 | 运维照提示登录 `/admin/` 必然失败，且系统实际上**没有任何可用的 Django 超级管理员** | 抽出 `manage.py ensure_superuser`：判定条件改为「是否已有**启用的超级管理员**」；已有则不动口令，没有才提升 `admin` 并设置口令（`ADMIN_PASSWORD` 或随机生成，仅打印一次）。凭据通过机器可读标记行输出给脚本，不再靠猜输出内容 |
| 2 | **全程没有 `chown`**。gunicorn / qcluster 以 `www-data` 运行，但 `/opt/tnr` 是 root 建的 | `media/` 不可写 → **捕捉照片、合照、签名上传全部失败**，而 `supervisorctl status` 显示一切正常 | 新增「6.5 目录属主与权限」步骤：代码 `root:www-data` 只读、`media/`+`staticfiles/` 归 `www-data` 可写、`.env` 收紧为 640 |
| 3 | `DB_PASS` 默认值 `ChangeMeInProduction!` 是公开弱口令，且该值被直接拼进 SQL 与 `.env` heredoc，含 `' $ ` 等字符会破坏解析 | 弱口令 + 特定字符下部署直接失败或配置被截断 | 未指定则自动生成随机口令；对显式传入的值做字符集校验，不安全字符直接拒绝并说明原因 |
| 4 | `cp -r . "$PROJECT_DIR/"` 把本地 `.git`、`.venv`、`db.sqlite3`、`server.log` 一起拷进部署目录 | 泄漏 git 历史、生产目录混入本地开发库，排障时误判 | 复制后显式清理这些本地痕迹 |

### 2.9.2 安全默认值缺陷（3 项）

settings.py 原来的三个默认值叠加起来，构成一个「安静地不安全」的组合：
`DEBUG` 默认 `True`、`SECRET_KEY` 有硬编码公开兜底、`ALLOWED_HOSTS` 默认 `*`。
只要 `.env` 丢失或有人绕过部署脚本手动起 gunicorn，应用就会在**看起来完全正常**的
状态下运行——堆栈与 settings 全公开、所有实例共用一把人人可查的密钥（会话可伪造）、
任意 Host 头都被接受。

| # | 缺陷 | 修复 |
|---|---|---|
| 5 | `DEBUG` 默认 `True` | 默认改为 `False`；开发机由 `.env` 显式开启 |
| 6 | `SECRET_KEY` 内置公开兜底值 | 去掉兜底。生产模式（`DEBUG=False`）下缺失即抛 `ImproperlyConfigured` 并给出生成命令；开发模式自动生成临时密钥，不牺牲便利性 |
| 7 | `ALLOWED_HOSTS` 默认 `*` | 生产缺失同样启动失败；开发模式回落到 `localhost/127.0.0.1/[::1]/0.0.0.0` |

同时补齐了原本完全缺失的 HTTPS 与 Cookie 配置（第 8 项）：

| 项 | 说明 |
|---|---|
| `SECURE_PROXY_SSL_HEADER` | nginx 反代下不配这项，`request.is_secure()` 恒为 `False`，后续所有 HTTPS 判断全部失真 |
| `HTTPS=on` 开关 | 联动 `SESSION_COOKIE_SECURE` / `CSRF_COOKIE_SECURE` / `SECURE_SSL_REDIRECT` / HSTS |
| `SESSION_COOKIE_SAMESITE='Lax'` | **显式声明**。业务接口普遍 `@csrf_exempt`，当前 CSRF 防护实际全靠这个默认值兜底，不能靠隐式默认 |
| `CSRF_TRUSTED_ORIGINS` | 通过域名访问时 Django 会校验 Origin/Referer，未列入会 403 |
| `LOGGING` | 补上生产日志配置，4xx/5xx 提级到 WARNING，由 supervisor 收进 `/var/log/tnr/` |

### 2.9.3 文档错误（1 项）

`DEPLOY.md` 让运维设置 `DJANGO_SECRET_KEY`，而代码读取的是 `SECRET_KEY`——
**变量名根本对不上**。修复前这会导致静默使用公开兜底密钥；现在会直接启动失败
（正是期望行为）。文档已修正，并补充了 HTTPS 启用步骤、目录属主命令与部署后自检清单。

### 2.9.4 本轮验证结果

| 验证方式 | 结果 |
|---|---|
| Django 全量自动化测试 | **450 个用例全部通过**（新增 9 个 `ensure_superuser` 用例 + 7 个安全配置用例） |
| 生产配置缺失即失败 | 子进程实测：缺 `SECRET_KEY` / 缺 `ALLOWED_HOSTS` 均非零退出且报错含具体项名 |
| 生产配置齐全 | `check --deploy` 从 6 条告警降到 2 条（仅剩 HSTS 子域/预载——这两项是不可逆承诺，**有意不自动开启**） |
| 开发模式便利性 | `DEBUG=True` 且未配 `SECRET_KEY` 时自动生成临时密钥，`manage.py check` 正常通过 |
| 部署凭据链路 | 用临时账号端到端跑通 `ensure_superuser` → deploy.sh 解析：首次输出 `ADMIN_CREDENTIALS=`，二次运行输出 `ADMIN_RESULT=SKIP` 且口令不变；验证后已清理，演示库状态复原 |
| 目录权限语义 | 临时目录实测 `chmod -R u=rwX,g=rX,o=` 后：venv 二进制**组仍可读可执行**、代码组可读、`.env` 组可读、项目根组可进入 |
| 数据库口令防护 | `Pa'ss` / `Pa$ss` / `Pa ss` / `` Pa`ss `` 均被拒绝，空值自动生成安全口令 |
| 新配置下运行期自检 | 登录页 200 / 首页 302 / 静态文件 200；响应头含 `X-Frame-Options: DENY`、`X-Content-Type-Options: nosniff`、CSRF Cookie `SameSite=Lax`；未授权 API 返回 **401 JSON**（不是 302 跳登录页） |
| Shell 脚本语法 | `bash -n` 通过（`deploy.sh`、`start_dev.sh`） |

### 2.9.5 本地 `.env` 收敛

本机 `.env` 原是从示例直接复制的，含 `ALLOWED_HOSTS=*` 与占位密钥
`SECRET_KEY=change-me-in-production`。已收敛为具体主机列表并重新生成随机密钥
（副作用：本地登录态失效一次，重新登录即可）。

> 保留项：`deploy/nginx.conf` 与 `deploy/supervisor.conf` 是 `deploy.sh` 内置内容的
> 独立副本，两者需要手工保持同步。目前内容一致，但存在漂移风险，建议后续改为
> 由 `deploy.sh` 单一来源生成。

---

## 二点十、前后端契约一致性 + 区县归属专项（2026-09-16 第五轮）

前四轮查的是「后端逻辑对不对」「部署路径安不安全」。这一轮换了个视角：
**后端是对的，但前端没把它用对**——用户最初反馈的「按钮不生效」「详情缺字段」
「状态全是已完成」，根因大多落在这一层。所以本轮把「模板/JS 源码」本身当作被测对象，
用静态断言把这类脱节锁死。

### 2.10.1 前端枚举映射表与后端 choices 脱节（5 处）

前端各端门户都自己维护了「枚举码 → 中文 + 徽章色」的映射表。后端新增枚举值时，
映射表不会自动跟上，界面上就直接漏出 `receive`、`pending_claim` 这样的英文枚举码。

| # | 位置 | 缺失的枚举 | 影响 |
|---|---|---|---|
| 1 | 医院端 `typeMap` / `typeBadge`（2 张表） | `receive`、`adjustment` | 医院签收下发物料时后端写 `type='receive'`（`views_material.py:275`），界面显示成英文 `receive` |
| 2 | 捕捉点端 `typeMap`（2 张表） | `receive`、`adjustment` | 同上；且物资流向**筛选下拉里根本没有这两项**，选不到 |
| 3 | 监管端 `txnTypeMap` | `receive` | 同上 |
| 4 | 捕捉点端 `statusBadge` | `pending_claim`、`cancelled`、`in_progress` | 领养「待领出」、领养「已撤销」、诊疗「进行中」全部漏英文码 |
| 5 | 监管端转运详情抽屉 | — | `value: r.status \|\| '—'` 直接输出枚举码，未做任何映射 |

> 这不是纸面问题：演示库实际存在 **2 条 `receive` 流水**，缺陷在真实数据上肉眼可见。

### 2.10.2 状态被静默兜底成「已完成」（用户报告第 6 条的真正根因）

`templates/portal/shelter/portal.html` 三处写成 `this.statusBadge(r.status || 'completed')`。
**当状态字段为空时，界面会谎报「已完成」**——这正是用户看到「状态全是已完成」的根因之一
（另一个根因是 `capture_create` 硬编码 `status='completed'`，已在二轮修掉）。

修复：去掉业务状态兜底，只保留 `—` / `未知` 这类中性占位；并新增静态测试
`NoHardcodedStatusFallbackTest` 用正则 `\bstatus\s*\|\|\s*['"]([A-Za-z_]\w*)['"]`
扫全部门户源码，**禁止任何业务状态码出现在兜底位置**。

另有一处**用错映射函数**：捕捉台账原本调用通用 `statusBadge`（缺 `partial`/`void`），
改为专用的 `captureStatusBadge`。

### 2.10.3 捕捉单区县误标 —— 跨模块数据不可见（本轮最严重）

**现象**：捕捉点登记的捕捉单，本区县政府在区县隔离下**看不到**。

**根因链**：

1. `capture_create` 写的是 `data.get('district_id') or user.district_id` ——
   **既无范围校验，也无机构区县兜底**；
2. 前端新增表单的区县默认值取的是 `user?.district_id`（操作员所属区县）；
3. 现场两个捕捉点操作员被挂在「全市（市级）」下（`district.is_city=True`）；
4. 而 `is_city=True` 的区县在隔离逻辑里**可见全部数据**，于是这些单落到市级，
   本区县政府反而看不到自己辖区的数据。

**实测影响**：现场 **8 条**捕捉单归属市级。

**三层修复**：

| 层 | 修复 |
|---|---|
| 后端 | 新增 `_resolve_district_scope(user, anchor, submitted)`：解析顺序为「显式提交 → **anchor 所在区县** → 操作员区县」；拒绝市级、拒绝跨区。`capture_create` / `capture_update` / `owner_return_create` 三处统一调用（捕捉单的 anchor 是捕捉点，主人领回的 anchor 是宠物） |
| 前端 | 新增表单区县下拉**排除市级**（`filter(d => !d.isCity)`）；默认值改为「当前捕捉点所在区县」；监管账号选定捕捉点后区县**自动跟随**；编辑弹窗下拉同样排除市级 |
| 数据 | 迁移 `0010` 把历史误标单改判回捕捉点所在区县，**并同步其宠物档案的区县** |

```python
def _resolve_district_scope(user, anchor, submitted):
    """解析并校验业务记录的归属区县。

    规则：
    1. 解析顺序：显式提交 → anchor 所在区县 → 操作员所属区县。
       anchor 的区县比操作员区县更贴近事实——现场把两个捕捉点操作员都挂在
       「全市（市级）」下，若以操作员区县为准，登记出来的捕捉单会全部落到市级，
       导致本区县政府在区县隔离下看不到本区数据。
    2. 必须是具体区县，不能是「全市（市级）」。
    3. 非市级操作员只能归属到自己的区县。
    """
```

### 2.10.4 北京地名残留（57 处）

第三轮只改了**区县名/机构名**，但机构地址、用户显示名、各类冗余名称副本
仍停留在北京时期。本轮做了一次全库扫描并按类型清理：

| 类型 | 示例 | 处理 |
|---|---|---|
| 机构地址（10 处） | `朝阳区建国路88号` → `襄城区檀溪路88号` | 种子数据修正 + 迁移 `0011` |
| 冗余名称副本（13 类字段） | `operator_name='朝阳捕捉点操作员'`、`from_shelter_name='朝阳区流浪动物捕捉点'` | 迁移 `0011` 按映射表替换 |
| 种子捕捉单地址 | `朝阳区建国路88号3栋` | 迁移 `0011` |
| 领养人地址 | `朝阳区某某小区` | 迁移 `0011` |

所有替换都带**「当前值仍等于已知旧值」的守卫**，人工改过的数据不会被覆盖。

### 2.10.5 种子数据的冗余副本自愈

`shelter_name` / `community_name` 是机构名的**写入时快照**，`get_or_create` 只在新建时
应用 `defaults`，机构后来改名后副本会一直停在旧值。`_seed_captures` 新增一次同步：
以机构当前名为准校准种子捕捉单的副本字段并打印清单，保证演示数据自洽。

### 2.10.6 本轮新增测试（33 个）

| 文件 | 用例数 | 锁定内容 |
|---|---|---|
| `core/tests_frontend_consistency.py` | 13 | 枚举映射覆盖（物料类型 / 各端状态）、禁止状态硬编码兜底、路由双向对齐、区县默认值规则 |
| `business/tests/test_migrations.py` | 16 | 迁移 `0010`/`0011` 的正确性、**不覆盖人工修改**、**幂等** |
| `business/tests/test_capture_views.py` | +4 | 未知区县拒绝 / 市级区县拒绝 / 默认落到捕捉点区县 / 跨区创建拒绝 / 市级账号可自选 |

`core/tests_frontend_consistency.py` 的实现要点（不启浏览器，把模板源码当被测对象）：

```python
def find_object_literal(source, var_name, occurrence=0):
    """返回 `const/let/var <var_name> = { ... }` 里 `{...}` 的完整文本。

    用「括号配对扫描」而不是正则——映射表里嵌套了数组/对象，
    正则会把内层 `}` 当成结尾，截出半个表，断言就形同虚设。
    """
```

```python
    MARK = '\x00'                                    # 拼接缝隙的标记符
    GAP = re.compile(r"""(['"`])\s*\+\s*[^+'"`]*?\s*\+\s*\1""")
    INTERP = re.compile(r'\$\{[^}]*\}')              # 模板字符串插值

    def _route_regex(self, route):
        parts = re.split(r'\{X\}|<[^>]+>', route)    # 把 <int:pk> 归一化
        return re.compile(self.MARK.join(re.escape(p) for p in parts))
```

> 为什么不做「字面量配对解析」：前端有 `'/api/business/captures/' + id + '/update/'`
> 这类拼接，源码里还有未配对的引号，正则会被带偏。改为**把拼接缝隙统一替换成标记符**，
> 再把后端路由做同样归一化后做子串匹配，稳定得多。

### 2.10.7 本轮验证结果

| 验证方式 | 结果 |
|---|---|
| Django 全量自动化测试 | **483 个用例全部通过**（`Ran 483 tests in 36.557s / OK`），四轮时为 450 |
| 约束力验证（变异测试） | 故意删掉 `receive` 映射 + 恢复一处 `status \|\| 'completed'`，测试正确报 `failures=2`；随后复原并复核 |
| 路由契约审计 | 后端 **77 条 `/api/` 路由全部有前端调用点**，前端无未注册路由调用 —— **无死接口、无死按钮** |
| 字段名契约审计 | 表单/JSON/查询参数三通道比对：**无任何前端提交字段被后端丢弃** |
| 权限矩阵审计 | AST 抽取视图装饰器链，无漏挂（`adoption_hall_*` 有意公开，`api_me` 手工返回 401） |
| `makemigrations --check` | `No changes detected` |
| `node --check` | 四个门户内联脚本语法全部通过 |
| 迁移实测 | `0010`：**已修正 8 条捕捉单的归属区县（市级 → 捕捉点所在区县）**；`0011`：清理冗余名称副本中的北京地名 |
| 全库地名扫描 | 北京地名残留 57 → **4 处**（仅剩本轮冒烟脚本自建的 `geo_address='朝阳区冒烟路100号'`，待用户确认后清理） |

真实 HTTP 冒烟（区县政府数据可见性，端到端）：

```
2. 捕捉单可见性（修复前 cy_gov 看不到 cy_shelter 登记的捕捉单）
  市级 admin           可见 10 条  分布={'襄城区': 9, '樊城区': 1}
  襄城区 cy_gov        可见  9 条  分布={'襄城区': 9}
  襄城捕捉点 cy_shelter 可见 10 条  分布={'襄城区': 9, '樊城区': 1}
  其中属于「襄城」捕捉点的: 9 条

3. 接口层规则验证
  提交市级区县 -> HTTP 400 success=False msg=归属区县不能是市级，请选择具体区县

4. 不提交区县时，捕捉单落到捕捉点所在区县
  创建成功 单号=CAP-260915-5414 district=襄城区 shelter=襄城流浪动物捕捉点

结论
  襄城区政府能看到本区捕捉单      : 是
  捕捉单中已无「全市（市级）」归属 : 是
```

> 本轮最大的价值不在修掉那 8 条数据，而在于**把「前端映射表必须覆盖后端全部枚举」
> 这件事变成了自动化断言**——以后新增枚举值忘了补前端，测试会直接失败。

---

## 二点十一、前端 GUI 浏览器实测（2026-09-16 第六轮）

前五轮覆盖了 API 层（自动化测试 + HTTP 冒烟）与模板源码静态层（源码断言），
**唯独没有真正把页面渲染出来跑一遍**。这一轮补上：用 Playwright 驱动真实 Chromium，
逐端逐页遍历，既验证用户反馈的 7 项缺陷，也抓取渲染期的 JS 报错。

结论先说：**抓到一个前五轮全部漏掉的真实缺陷**——它语法完全合法、接口测试碰不到、
源码静态断言也看不出，只有把页面渲染出来才会暴露。

### 2.11.1 新发现的真实缺陷：放养页「待回收」标签页整个空白

| 项 | 内容 |
|---|---|
| 现象 | 捕捉点端「动物回收放养 → 待回收」页签**空白**，无表格、无报错提示 |
| 报错 | `ReferenceError: rowActions is not defined`（`renderRelTab`） |
| 根因 1 | 该分支写了简写选项 `{ ..., rowActions }`，但 `rowActions` **只声明在同函数的 `released` 分支里** —— 同文件内有声明，但不在同一作用域 |
| 根因 2 | 同一处 `TNR_UI.mountTable('[data-mt="…"]', …)` 写在 `wrap.innerHTML = html` **之前**，容器还没进 DOM，选择器查不到元素（即便修掉根因 1，表格也仍然不会渲染，且**完全不报错**） |
| 修复 | 删除多余的 `rowActions`（该表已有独立的「操作」列）；把 `wrap.innerHTML = html` 提到 `mountTable` 之前；顺带把 `released` 分支改成同一写法（原先直接 `mountTable(wrap, …)`，与其他分支不一致） |

为什么前五轮都没发现：这类缺陷**不产生接口错误、不影响任何 API 断言**，
页面只是安静地少渲染一块内容。API 层测试与源码静态断言在原理上就覆盖不到。

### 2.11.2 新增 2 个源码级回归测试（锁住这一类缺陷）

修复之外，把「渲染时序」与「作用域」两类问题做成了静态断言，加入
`core/tests_frontend_consistency.py`：

| 用例 | 锁定内容 |
|---|---|
| `RenderContainerOrderingTest` | `mountTable('[data-mt="X"]', …)` 之前必须存在**真实的** `.innerHTML =` 赋值（注释不算） |
| `UndeclaredOptionIdentifierTest` | `mountTable` 选项里的简写标识符必须在**同一方法作用域**内声明（跨分支误用要能抓出来） |

实现上踩了三个坑，都在代码注释里写明了原因：

1. **取对象字面量要用括号配对扫描**，正则会截出半张表；
2. **判定「简写标识符」要同时看前后字符**：前一个非空白字符必须是 `{` 或 `,`，
   后一个必须是 `,` 或 `}`。只看「后面不是冒号」会把 `title: title` 的**值**、
   `this.state.supTab` 里的 `this`/`state`、`arr.length` 里的 `length` 全当成简写；
3. **注释必须先剥掉**：我给根因 2 写的修复说明注释里就含 `wrap.innerHTML = html`
   字样，直接把时序检查骗过了。剥离时还要注意**必须从文件开头扫描**（从中间片段
   开始会让引号配对失步）且**保持长度不变**（索引与行号才能继续复用）。

**变异测试验证约束力**：把代码还原成修复前的写法，两个用例**都正确失败**并给出
`rowActions` 未声明与容器时序错误；复原后重新全绿。

### 2.11.3 捕捉端功能实测（31 项检查，0 失败）

逐条对应用户最初反馈的 7 项缺陷，在真实浏览器里操作并断言：

| 用户反馈 | GUI 实测断言 | 结果 |
|---|---|---|
| ② 小区搜索是文本输入 | `[data-filter="community"]` 是 `INPUT[type=text]`（占位文案「输入小区名称，回车搜索」）；「区县」仍为 `SELECT`（对照项未被误改） | ✅ |
| ② 回车提交 | 输入关键词按回车 → 列表从 10 行过滤到 3 行且全部命中；「重置」恢复 10 行 | ✅ |
| ① 定位地址作为默认地址 | 授予定位权限 + 模拟坐标后点「定位」→ 地址框从空自动填入 `湖北省襄阳市襄城区余家湖街道岘山路932号湖北岘山国家森林公园`（真实调用高德逆地理接口成功） | ✅ |
| ① 提交保存到数据库 | 生成编号 → 画签名 → 提交，提示「捕捉登记成功！单号：CAP-260916-4879」；回查接口确认已入库，`geo_address`、`经纬度`、`address` 三项均非空 | ✅ |
| ③ 详情含新增表单全部内容 | 详情抽屉 18 个字段全部呈现（含整体合影、单只照片、物业电子签名、定位地址、经纬度、转运情况） | ✅ |
| ④ 编辑：小区为文本输入 | 编辑弹窗 `#ed_communityName` 是 `INPUT[type=text]`；物业名/交接人/电话/地址均可编辑；区县下拉同样排除市级 | ✅ |
| ④ 已转运不可编辑 | 5 条已转运记录的「编辑/删除」按钮带 `disabled` 与提示「该捕捉单已有动物提交转运，需医院全部退回后才能操作」；**绕过 disabled 强制点击**也被 JS 层拦截并提示 | ✅ |
| ⑤ 取消按钮生效 | 编辑弹窗「取消」→ 弹窗数 1 → 0；删除确认框「取消」→ 记录未被删除（10 条不变） | ✅ |
| ⑥ 状态不能全是已完成 | 列表状态分布 `待转运×5 / 部分转运×3 / 已完成×2`；无英文枚举码漏出 | ✅ |
| ⑦ 删除：逻辑删除 | 确认框明确写「删除为逻辑删除：数据保留可追溯」；删除后从列表消失 | ✅ |

**逻辑删除的 ORM 复核**（关键语义，浏览器看不到）：

```
#14 CAP-260916-4879  is_deleted=True  status=void
     deleted_by=cy_shelter（捕捉点操作员）  deleted_at=2026-09-16 01:37:14
     地址/定位地址/经纬度 全部保留    电子签名保留 9798 字符
     其下宠物 TNR260916002 一并 is_deleted=True
```

→ **数据库行仍在、业务内容完整保留、仅标记作废**，确认不是物理删除。

### 2.11.4 四端全站巡检（24 个页面，0 失败）

逐端登录后遍历侧边栏全部页面，做两件事：捕获渲染期 JS 报错、扫描是否漏出英文枚举码。

| 端 | 账号 | 遍历页面 | 枚举泄漏 | JS 报错 |
|---|---|---|---|---|
| 捕捉点端 | cy_shelter | 10 | 无 | 无 |
| 医院端 | babitang_hosp | 8 | 无 | 无 |
| 区县政府端 | cy_gov | 6 | 无 | 无 |
| 领养人端 | adopter1 | 1（无侧边栏） | 无 | 无 |

「枚举泄漏」的判定是精确的：扫描所有**叶子节点**，若其文本恰好等于某个后端枚举码
（`receive`/`pending_claim`/`in_progress` 等 26 个）即判为泄漏。这直接证伪了第五轮
修的 5 张映射表——静态断言只能证明「源码里有那个 key」，**渲染结果才能证明界面上不漏码**。

### 2.11.5 环境踩坑记录（对后续排查很有用）

| 坑 | 表现 | 原因与对策 |
|---|---|---|
| **Django 5.0 模板缓存 + `--noreload`** | 改了模板文件，服务端仍返回旧内容，误以为「修复无效」 | Django 5.0 起**恒定**把模板加载器包进 `cached.Loader`（不再看 `DEBUG`）；缓存失效依赖进程重启。用 `runserver --noreload` 时**任何模板改动都必须重启进程**。本次为此浪费了一轮排查 |
| 验证探针本身失效 | 在文件末尾追加 `<!-- 标记 -->` 后服务端仍无标记，误判为缓存 | 模板是 `{% extends %}` 结构，追加到 `{% endblock %}` 之外的内容**根本不会被渲染**。探针必须落在块内 |
| 提示文案抓不到 | 用 `window.TNR_UI.toast` 包装钩子，结果永远抓不到任何提示，误判为「3 项功能失效」 | `TNR_UI` 用 **`const`** 声明，不挂 `window`，包装无效。改用 `MutationObserver` 观察 `.toast` 节点，不依赖应用内部实现 |
| 签名画不上 | 提交被「请完成物业电子签名」拦住，看起来像后端缺陷 | `page.mouse` 用的是**视口坐标**，签名板在表单底部（视口外）时事件落空。必须先 `scrollIntoViewIfNeeded()`，并断言 canvas 真的被画上了像素 |
| 同一会话换账号登录失败 | 第二个账号登录时 `#id_username` 超时 | 已登录状态下再 `GET /login/` 会被重定向，拿不到登录表单。每个账号必须用**独立 browser context** |
| 脚本找不到依赖 | `Cannot find module 'playwright'` | Node 按**脚本所在目录**解析模块，不是 cwd。需设 `NODE_PATH` 指向隔离依赖目录 |

> 记录这些的意义：它们全都是「看起来像产品缺陷、其实是测试脚手架缺陷」的形态。
> 先分清「产品缺陷」和「脚手架缺陷」，否则会误报一堆不存在的问题。

### 2.11.6 本轮验证结果

| 验证方式 | 结果 |
|---|---|
| Django 全量自动化测试 | **485 个用例全部通过**（五轮 483 + 本轮新增 2 个静态回归用例） |
| 捕捉端 GUI 功能实测 | **31 项检查，0 失败** |
| 四端 GUI 全站巡检 | **12 项检查，0 失败**（24 个页面无 JS 报错、无枚举泄漏） |
| 变异测试 | 还原缺陷写法后两个新用例**均正确失败**，复原后全绿 |
| 逻辑删除 ORM 复核 | 行保留、内容完整、标记作废、子记录级联作废 —— 确认非物理删除 |
| 截图 | `gui-test-screenshots/90~` 系列（四端门户首页、定位后表单、提交成功、详情、编辑弹窗、删除确认、列表终态） |

### 2.11.7 本轮操作前后备份

- GUI 实测前备份：`db.sqlite3.bak-gui-20260916-023229`

### 2.12 测试数据清理与区县名更正（2026-09-16 收尾）

前六轮测试在现场库里留下了若干测试痕迹。用户确认后统一清理，并更正一处区县名笔误。

#### 2.12.1 清理前的引用审计

清理前先把**全库所有模型**过了一遍，逐条判断「演示数据 / 测试痕迹」，再核对
反向引用（含 `related_objects` 遍历）与媒体文件引用，确认删除不会留下孤儿行：

- 演示数据的时间戳高度一致（`2026-09-13 09:15:58` 一次性导入），
  与测试痕迹（09-13 10:39 之后）在时间轴上天然分界，据此逐条归类。
- 测试痕迹共命中 5 张表，另有 5 个媒体文件**无任何记录引用**。

#### 2.12.2 清理清单（物理删除，均为测试产物）

| 表 | 删除 | 内容 |
|---|---|---|
| `business.Capture` | 12 条 | `#3–#14`：二轮走查单、四/五轮冒烟单、六轮 GUI 实测单（含已逻辑删除的 4 条） |
| `business.Pet` | 19 条 | `#7–#22`、`#33–#35`，上述捕捉单的宠物档案 |
| `business.Transfer` | 5 条 | `#4`（`capture` 为空的历史孤儿单）+ `#5–#8` 冒烟转运单 |
| `business.MaterialTransaction` | 1 条 | `#9` 备注 `SMOKECHIP-001 芯片植入` 的消耗流水 |
| `business.Blacklist` | 4 条 | `#2–#5`「冒烟黑名单改」 |
| `media/photos/` | 5 个文件 | 4 个 16 B 垃圾图 + 1 张 314 KB 截图，**无任何 ImageField 引用** |

删除顺序为「转运单 → 宠物 → 捕捉单」：`Transfer.capture` 与 `Pet.capture` 都是
`SET_NULL`，先删父表会留下 `capture_id=NULL` 的孤儿行。

#### 2.12.3 保留项（演示/种子数据）

`Capture #1–#2`、`Pet #1–#6`、`Transfer #1–#3`、`Treatment #1–#4`、
`Release #1`、`Adoption #1`、`CheckIn #1–#2`、`Euthanasia #1`、
`MaterialTransaction #1–#8`、`Blacklist #1`、`Message #1–#3`、
`AdoptionHallListing #1`、`Chip` 500 条、`Material` 4 条、
`District` 5 条、`Institution` 10 条、`User` 9 条。

> `AdoptionHallListing #1` 虽产生于测试时段，但它是 `promote_adoptable`
> 对演示宠物 `TNR2501003`（`pending_adopt`）的**正当产物**——真实定时任务同样会
> 生成这条上架记录，且领养大厅页面依赖它展示。判定为演示数据，保留。

#### 2.12.4 区县名更正：`东经开发区` → `东津新区`

襄阳市实际的功能区是**东津新区**（另有襄阳高新技术产业开发区、鱼梁洲开发区），
不存在「东经开发区」——「经」是「津」的拼音笔误（jīng / jīn）。

- 新增数据迁移 `business/0012_rename_dongjing_district`，带守卫（只在当前值仍等于旧值时改写），
  已部署环境重跑 `migrate` 即可同步；
- `seed_data.py` 同步更正（区县名 + `I006`/`C004` 两处地址），保证全新部署名称正确；
- 机构 `C004` 的名称「开发区和谐家园」**未改**——它是可独立成立的社区名，
  且东津新区在口语中本就被称作「东津开发区」。

> **为什么迁移放在 `business` 而不是 `core`**：本迁移要改写 `core.Institution.address`，
> 而 `business/0011` 恰好也会把 `I006`/`C004` 的地址写成「东经开发区和谐路X号」。
> 若挂在 `core` 应用下，它只依赖 `core/0003`，与 `business/0011` **无依赖关系、
> 执行顺序不确定**——一旦本迁移先跑、`0011` 后跑，笔误地址会被**重新写回来**。
> 挂到 `business/0011` 之后即可保证顺序，这也与既有惯例一致（`0010`/`0011` 同样是
> 「地名修正」，同样在 business 应用下改 core 模型）。

> 顺带确认了 `seed_data` 的一个安全设计：区县名**只在新建时写入**，
> 已存在则保留现有名称并打印提示（`_seed_districts` 的 `d.name != name` 分支）。
> 因此人工改名后重跑种子不会被回退——本次改完实测连跑两次，名称稳定为「东津新区」。

#### 2.12.5 清理后验证（全部通过）

| 检查 | 结果 |
|---|---|
| 17 个模型计数 | 与预期**逐项一致**（`Capture` 2 / `Pet` 6 / `Transfer` 3 / `Blacklist` 1 / `MaterialTransaction` 8 …） |
| 孤儿/悬空引用 | **0 处**（转运单的 `capture`、宠物编号与 `capture` 归属全部可解析） |
| 全库文本残留扫描 | **0 处**（`冒烟`/`GUI实测`/`走查路`/`达摩院`/`SMOKECHIP`/`朝阳`/`海淀`/`西城`/`东城`/`东经` 全部清零） |
| 派生状态一致性 | `Capture #1`=partial（4 只 / 已转运 3）、`#2`=completed（2 只 / 已转运 2），与推导结果一致 |
| 媒体目录 | 无未引用残留（5 个测试文件已移至 `media/.cleanup-20260916-094612/`，可恢复） |
| 种子幂等性 | 连跑两次 `seed_data`，20 个模型计数**零变化**，区县名未被回退 |
| Django 全量测试 | **485 个用例全部通过** |
| `makemigrations --check` | `No changes detected` |
| **全新库复现验证** | 指向全新 SQLite 跑 `migrate` + `seed_data`，**17 项计数与清理后的现场库逐项完全一致**，残留扫描 0 处 |

**全新库复现验证（最有说服力的一项）**：用一份临时 settings 把数据库指向全新文件，
跑完整 `migrate` + `seed_data`，得到：

```
Capture 2 / Pet 6 / Transfer 3 / Treatment 4 / Release 1 / Adoption 1 / CheckIn 2
Euthanasia 1 / Blacklist 1 / Message 3 / AdoptionHallListing 1
Material 4 / MaterialTransaction 8 / Chip 500
District 5 / Institution 10 / User 9
区县：全市（市级）/ 襄城区 / 樊城区 / 东津新区 / 襄州区
I006 地址=东津新区和谐路8号   C004 地址=东津新区和谐路6号
```

这组数字与**清理后的现场库完全一致**，同时说明三件事：

1. 清理把现场库恢复到了**种子原始状态**（不多不少）；
2. `seed_data` 完全可复现，演示业务数据（捕捉/宠物/转运/诊疗/放养/领养/打卡/黑名单/消息）
   全部由种子生成，不是手工攒出来的；
3. 全新部署链路（`migrate` → `seed_data`）产出的区县名与地址**正确**，
   迁移顺序隐患已消除。

> 这也正好呼应了用户「区县未来会全部清除、全新创建」的计划——
> 现在从零重建一条命令即可，且结果与当前库一致。

#### 2.12.6 备份

- 清理前备份：`db.sqlite3.bak-20260916-094505-before-cleanup`

---

## 二点十三、权限矩阵穷举 · 区县隔离 FieldError（2026-09-16 第八轮）

前七轮的权限测试覆盖的是**代表性角色组合**，不是穷举。本轮补「全部路由 × 全部角色」
的笛卡尔积矩阵（用临时库 + 不存在的 ID 探测，不误改数据）。

### 2.13.1 发现的缺陷

**`gov_district`（区县政府角色）访问两个接口直接 500（`FieldError`）**：

| 接口 | 说明 |
|---|---|
| `/api/business/checkins/` | 回访打卡列表 |
| `/api/business/hall-listings/` | 领养大厅上架列表 |

**根因**：`CheckIn` 和 `AdoptionHallListing` 模型**本身没有 `district` 外键**，
但 `get_district_filtered_queryset(model, user)` 原先直接 `model.objects.filter(district_id=…)`，
对这些模型抛 `FieldError`。

**为什么前七轮全部漏掉**：市级用户走 `return all()` 分支绕过了过滤逻辑，
只有**区县角色**才走到 `filter(district_id=…)` 分支 —— 而前七轮主要用
`gov_city` / `shelter` / `hospital` / `adopter` 角色，对 `gov_district` 的覆盖
仅限于有针对性越权测试，没有用区县角色去访问**所有**列表接口。

### 2.13.2 修复

- `business/models.py`：两个模型声明类属性 `DISTRICT_LOOKUP = 'pet__district'`
  （区县沿所属宠物派生，与 `supervision/views.py` 既有做法一致）
- `business/services.py`：新增 `district_lookup_path(model)`，
  `get_district_filtered_queryset()` 改为按 `f'{lookup}_id'` 过滤
  （默认 `district`，有 `DISTRICT_LOOKUP` 时用声明值如 `pet__district_id`）
- `get_scoped_object()` 间接受益（内部调用 `get_district_filtered_queryset`）
- 新增 `business/tests/test_district_scope_regression.py`（6 个用例）：
  区县角色不报错 + 数据按区县收敛 + 市级看全部

### 2.13.3 验证结果

- 全量测试 **491 通过**（485 + 6 新增）
- 全库扫描确认：**0 个模型既无 `district` 字段、也无法间接到达**

---

## 二点十四、捕捉端 10 项需求改造 + 政府端一宠一档（2026-09-16 第九轮）

用户一次性提出 10 条捕捉端改造需求（含政府端「一宠一档」）。

### 2.14.1 需求落地清单

| # | 需求 | 实现要点 |
|---|---|---|
| 1 | 看板「累计捕捉」改为**单据数/动物数双显** | `N 单 / M 只`；「单 / 只」单位小字、数字大字 |
| 2 | 「转运情况」+「状态」两列合并为**「转运状态」** | 四态：`未转运` / `x/y 转运中` / `x/y 部分完成` / `完成` |
| 3 | 列表去掉「详情」按钮，**点单号弹详情** | `ledgerNo` 列加 `data-capture-detail`；`rowActions` 只留编辑/删除 |
| 4 | 详情中的**照片点击可放大** | 详情抽屉内图片加 `data-zoom`，绑定 `photoZoom()` |
| 5 | 编辑时**单只动物照片缺失导致无法编辑** | 编辑弹窗补单只照片上传控件 + 后端 `pet_photo_<code>` 接收 |
| 6 | 转运交接改选**「待转运宠物」** | 过滤掉已有活跃转运单的宠物；点编号弹该动物**捕捉时的单只照片** |
| 7 | 转运单号可点弹详情 + 新增**「撤回」** | 新接口 `transfers/<pk>/withdraw/`；医院未签收可撤，宠物回待转运可重选 |
| 8 | 「动物回收放养」→**「动物去向」**四标签 | 回收 / 放养 / 领养 / 死亡 |
| 9 | 回收单表单字段 | 回收单号（系统生成）、回收人姓名、手机号、住址、签字、走失原因、时间 |
| 10 | **去掉安乐死备案** | 导航项、页面、`render_euthanasia` 全删；`已安乐死` 文案统一为「已死亡」 |

### 2.14.2 本轮发现的真实缺陷（6 处，均为前八轮未覆盖的形态）

| # | 位置 | 根因 | 后果 |
|---|---|---|---|
| 1 | `Transfer.STATUS_CHOICES` | 撤回写入 `status='void'`，但 choices 里**没有 `void`** | 状态值超出枚举定义 |
| 2 | `OwnerReturn` 模型 | 需求要求的「住址 / 时间」后端**根本没接收** | 用户填的数据被静默丢弃 |
| 3 | `showCaptureEdit` | 方法内**没有定义 `wrap`**（它是 `render_capture_list` 的局部变量） | 保存时 `ReferenceError`，编辑中断 |
| 4 | `showCaptureEdit` | 单只照片控件引用了**作用域外的 `pets`** | 弹窗渲染即崩，整体打不开 |
| 5 | `shelter_base.html` | 侧栏「动物回收放养」/「安乐死备案」是**静态硬编码**，只改了 `portal.html` 的 JS `navGroups` | 改名后的「动物去向」**点不进去**，被删的「安乐死」仍显示 |
| 6 | `gov_base.html` | `navMap` 有死键「全业务监管」，侧栏无此项 | 该页从侧栏**无法访问**（既有缺陷） |

> **缺陷 5 的教训**：`base` 模板侧栏与 JS `navMap` 是**成对**的，改导航名必须两侧同时改。
> 只改 JS → 页面点不进去；只改 base → 按钮指向不存在的页。已写入项目长期约定。

### 2.14.3 后端补强

- `services.capture_transfer_state()` / `capture_states_bulk()` 补 `received` / `settled`
  字段 —— 否则前端「部分完成 / 完成」是**死代码**（`st.received` 恒为 0，永不触发）
- `transfer_withdraw` 的裸查 `Transfer.objects.get(id=pk)` 改走 `get_scoped_object`（区县范围校验）
- 迁移 `business/0013`：`OwnerReturn` 加 `owner_address` / `return_time`
- 迁移 `business/0014`：`Pet.status` choices 文案 `已安乐死` → `已死亡`

### 2.14.4 本轮新增测试（26 个）

| 文件 | 数量 | 内容 |
|---|---|---|
| `business/tests/test_transfer_views.py` | +11 | 撤回权限 / 状态守卫 / 宠物状态回滚 |
| `business/tests/test_capture_views.py` | +4 | 回收单住址与时间落库 |
| `business/tests/test_services.py` | +9 | `capture_transfer_state` 四态统计 |
| `core/tests_frontend_consistency.py` | +1 | `ModalMethodOuterScopeLeakTest`：弹窗方法内**不得引用外层作用域变量**（锁住缺陷 3 / 4 的通用形态） |

**变异测试**：临时把 `const pets = full.pets || []` 改名为 `petsZZZ` 并把
`document.querySelectorAll` 改成 `document.querySelectorAllZZZ`，新测试**精准报出**
两个问题（含方法名与行号）；复原后全绿 —— 确认约束力真实有效。

### 2.14.5 GUI 实测（Playwright + 独立临时库）

用独立临时库 `gui_tnr.sqlite3`（不碰演示数据）逐条验证：

| 验证项 | 结果 |
|---|---|
| 看板双显 | ✅ `2 单 / 5 只 累计捕捉` |
| 转运状态列四态 | ✅ `未转运` / `1-2 部分完成` / `完成` 均可达 |
| 点捕捉单号弹完整详情 | ✅ 抽屉含 18 个字段 |
| 详情照片点击放大 | ✅ 端到端（临时库挂真实图片后验证） |
| 编辑单只照片上传 | ✅ 控件渲染 → 上传 → 落库 `/media/photos/…` |
| 转运撤回全链路 | ✅ 撤回后宠物回 `in_transit`，可重新选入其他转运单 |
| 回收表单 | ✅ 4 标签齐全、7 字段齐全、生成 `RET-` 单号 |
| 回收单落库 | ✅ 住址 / 时间 / 签名 / 区县（襄城区）全部正确，宠物状态 = `owner_returned` |
| 「已死亡」文案 | ✅ 四端统一，无「安乐死」残留 |

> **脚手架教训**：多处「功能失效」实为**探测脚本漏查 `drawer`**（只查了 `modal`）——
> 产品功能正常，是探针不完整。**先分清产品缺陷与脚手架缺陷**。

### 2.14.6 本轮验证结果

- 全量测试 **517 通过**（491 + 26）
- `manage.py check` 无问题；`makemigrations --check` = `No changes detected`
- 迁移 `0013` / `0014` 已应用；演示库 8 个模型计数与预期一致
- 四端内联 JS `node --check` 全通过
- 清理：临时库 `gui_tnr.sqlite3`、占位图（已移到 `/tmp/gui_backup/`）

### 2.14.7 本轮操作前后备份

- 迁移前备份：`db.sqlite3.bak-20260916-235241-before-r9-migrate`

---

## 二点十五、台账详情 / 看板 / 转运交接 / 驳回回池（2026-09-18 第十轮）

用户一次性提出 4 条需求。

### 2.15.1 需求落地清单

| # | 需求 | 实现要点 |
|---|---|---|
| 1 | 台账中心捕捉单据详情**只有合影，没有单只的信息和照片** | `business_supervision` 补 `pets` 字段；捕捉端 `renderCapturePets` 与政府端 `renderPetsList` 共用逐只明细（编号 / 昵称 / 状态 / 类别·品种·性别·毛色 / 芯片号 / 所属医院 + 全部阶段照片，编号可点开一宠一档） |
| 2 | 首页看板：快捷操作置顶（尤其手机端）、加「回收登记」、数据放下、**数据要有时间条件** | 卡片顺序改为 快捷操作 → 待办任务 → 数据概览；快捷操作 5 项（含「回收登记」深链 `release/recovery`）；数据概览加 今日 / 近7天 / 本月 / 近30天 / 全部 五个范围按钮，业务量受范围影响、待办保持实时 |
| 3 | 「当前待转运为何是 0」+ 检查其他数据块计算逻辑 | 「待转运」口径由 pending **单据笔数**改为**待安排转运的动物数**；「待领养审核」改查 `AdoptionApplication`（原过滤条件恒不成立，死代码） |
| 4 | 转运交接：「待签收」→**待转运**；已签收/已驳回/已撤回合并为**转运明细**；加时间/医院/宠物编号/状态搜索；按单据状态决定能否撤回 | 4 标签 → 2 标签；明细 5 个筛选；行内「撤回」仅 `pending` 单据显示 |
| 5 | 被驳回单据中的宠物**自动进入待转运备选框**；取消「重新下发」 | `_availableTransferPets` = 在途 且 未被 pending 单占用（`rejected`/`void` 不占位）；`transfer_resend` 的视图 / 路由 / API 客户端 / 测试 / 守卫清单全部移除 |

### 2.15.2 本轮发现的真实缺陷（6 处）

| # | 位置 | 根因 | 后果 |
|---|---|---|---|
| 1 | `business_supervision` | captures / transfers 只输出 `group_photo`，**没有 `pets` 字段** | 政府端单据详情「只有合影」，单只信息与照片全缺（用户报的就是这条路径） |
| 2 | 看板「待转运」 | 统计 pending 状态的**单据笔数** | 从没下发过单据的捕捉点**恒为 0**，而实际有 7 只动物在途待安排 |
| 3 | 看板「待领养审核」 | `Adoption.STATUS_CHOICES` 只有 `pending_claim/completed/cancelled`，前端却按 `=== 'pending'` 过滤 | 恒为 0 的**死代码** |
| 4 | `hospital_pets` vs `transfer_create` | 前端按 `user.district` 过滤（捕捉点操作员挂在市级 → 返回全市），后端按**机构区县**校验 | 备选框 7 只里只有 1 只能下发，其余必然报「不属于本区县」——**本轮改造自身引入的回归**，由 GUI 实测抓出 |
| 5 | `Transfer` 模型 | 接口 docstring 声明接受 `note`、前端「新建转运单」也提交，**模型没有这一列** | 用户填的备注被静默丢弃，详情抽屉「备注」恒为 `—` |
| 6 | `tnr-api.js` | 在 4 个 portal 模板里各自引入，且**都没有 `?v=`** | 改了它用户永远读浏览器缓存旧文件 |

> **缺陷 4 的教训**：前端「可选项列表」的过滤条件必须与后端写入时的校验条件**逐项对照**。
> 只加后端校验、不同步前端过滤，就会造出「必然被拒绝的死选项」——比空列表更糟。
>
> **缺陷 5 的教训**：接口文档 / 前端提交的字段，要跟模型字段做一次**集合比对**，缺一处就是静默失败。

### 2.15.3 后端补强

- `services.busy_transfer_codes(codes, district_id)`：返回已被**未结**转运单占用的编号
  （未结 = `ACTIVE_TRANSFER_STATUSES` = pending / received；`rejected` / `void` 不占位）
- `transfer_create` 两段式预校验（全部在写库之前）：重复下发拦截 + 跨区县拦截
- `hospital_pets` 对 `shelter` 角色改按 `user.institution.district_id` 过滤
- 迁移 `business/0015`：`Transfer` 加 `note` 字段
- `transfer_resend` 视图与路由移除

### 2.15.4 本轮新增测试

| 文件 | 内容 |
|---|---|
| `business/tests/test_transfer_views.py` | 删 4 个 resend 用例；新增 驳回回池 / 重复下发拦截 / 跨区县拦截 / 接口已下线 / note 落库 / note 默认值 共 6 个 |
| `core/tests_frontend_consistency.py` | 扩到 28 个用例；`FLOW_ENTRY_POINTS` 移除「转运重新下发」 |

### 2.15.5 GUI 实测（`gui-test-scripts/36_four_requirements.js`）

23 项检查全部通过，含端到端「下发 → 医院驳回 → 自动回备选框」：

| 验证项 | 结果 |
|---|---|
| 看板卡片顺序 / 快捷操作含「回收登记」/ 时间按钮 | ✅ `["快捷操作","待办任务","数据概览"]`、5 项快捷操作、5 个范围按钮 |
| 看板「待转运」不再是 0 | ✅ `hd_shelter` 显示 1（旧口径按 pending 单数 = 0） |
| 备选框限定本机构区县 | ✅ 可见宠物 9 只，覆盖区县 id = `[3]`（樊城区） |
| 转运交接标签 | ✅ `["待转运","转运明细"]` |
| 转运明细筛选 / 撤回按钮按状态 | ✅ 5 个筛选；撤回按钮仅出现在 `pending` 单据 |
| 台账详情逐只明细 | ✅ 捕捉端 `CAP-260917-8198`「动物明细（2只）」+ 3 张可放大图片；政府端 `CAP-260917-2519` 同 |
| 端到端驳回回池 | ✅ `TRF-260918-2676` 下发 → 驳回 → 动物回到备选框，看板同步 |
| 备注落库并显示 | ✅ 详情抽屉里能找到表单填的备注 |
| 「重新下发」已移除 | ✅ 页面无该字样，`TNR_API.resendTransfer === undefined` |

截图归档 `gui-test-screenshots/36*.png`（8 张）+ `36_four_requirements.log`。

### 2.15.6 本轮验证结果

- 全量测试 **589 通过**（587 + 备注 2 条）
- `makemigrations --check` = `No changes detected`；迁移 `0015` 已应用
- 实测数据已清理：转运单回到 15 张，无 `GUI实测` 痕迹残留
- 顺带修正：`TNR260917018` 那条「在途却挂着医院」的陈旧归属

### 2.15.7 本轮操作前后备份

- 迁移前备份：`db.sqlite3.bak-20260918-014340-before-r10-gui`

---

## 二点十六、列表冻结与去汇总标题（2026-09-18 第十一轮）

### 2.16.1 需求

1. **搜索条件与列表表头一并冻结**，仅允许列表内容区域滚动翻页。
2. **移除所有列表表头上方的汇总数据文字**（如「捕捉记录（12）」这类带数量的标题展示）。
3. 覆盖全部框架中的列表，冻结与去汇总行为一致。

### 2.16.2 现状实测（先复现，再动手）

写 `gui-test-scripts/37_freeze_diag.js` 遍历「三端 × 全部导航项 × 全部子标签」，
逐个量测筛选条与表头的视口坐标、滚动容器与 overflow 计算值。抓到的硬事实：

| 观测 | 结果 |
|---|---|
| 每个列表的 `.table-wrapper` | `overflow-x/y` 计算值均为 **`auto/auto`** |
| `thead th` 的 `position` 计算值 | **`relative`**（不是 `sticky`） |
| 带筛选条且可滚动的页面 | 表头随内容上移，`动物去向/回收` 表头从 `231` 掉到 `-32` 并被筛选条压住 |
| 全部 14 个列表组合 | 表头上方均存在带数量的标题（如 `捕捉记录（12）`） |

### 2.16.3 根因（两条叠加）

1. **同选择器、后者胜**：`.data-table thead th { position: sticky }` 被同文件后段的
   `.data-table thead th { position: relative }` 覆盖（后者是为列宽拖拽手柄
   `.dt-colresizer` 提供定位祖先而加）。
2. **滚动容器把 sticky 架空**：`.table-wrapper` 带 `overflow-x: auto`。
   CSS 规定一个轴不是 `visible` 时，另一个轴的 `visible` 会计算成 `auto` ——
   wrapper 因此成了滚动容器；而它的高度由内容撑开、永远不滚动，
   sticky 表头便彻底失效。

于是用户看到的现象成立：**筛选条钉住了，表头却跟着内容走，最后钻到筛选条底下**。

### 2.16.4 修法

**表头冻结** —— 让「列表内容区」成为唯一的纵向滚动区：

- CSS：给 `.dt-card .table-wrapper` 保留 `overflow: auto`（宽表仍可横向拖），
  用**更具体的选择器** `body .dt-card .data-table.dt-table thead th` 把表头钉回
  `position: sticky; top: 0`（不删那条 `relative`，避免影响手写的非 `dt-table` 表格），
  并加 `inset` 阴影补分隔线（`border-collapse: separate` 下 sticky 表头的下边框会随内容滚走）。
- JS：`TNR_UI._fitTableHeight()` 按视口给 wrapper 算 `max-height`
  = 视口高 − 固定头部 − 分页条高 − 卡片下方留白 − 呼吸位，
  使页面滚到底时列表恰好铺满剩余视口（表头正好停在筛选条下沿、分页条不被顶出屏幕）。
  `max-height` 只封顶不拉伸，短列表观感与改动前一致。

**去汇总标题** —— `mountTable` 不再渲染列表卡标题头（`title` + `actions`），
38 个调用点里的 `title:` 与 5 处 `actions:` 一并清除（不留死参数）。

### 2.16.5 过程中实测抓到的三个坑

| 坑 | 现象 | 处理 |
|---|---|---|
| 高度算早了 | 「账号权限管理」算出 `max-height=199px`，手动重算只有 `137px` —— `mountTable` 时列表之外的卡片（角色权限说明）还没渲染完，`tail` 偏小 | 用 `MutationObserver`（**仅 `childList`**，不含 attributes）观察内容区，任何后续渲染都触发去抖重算 |
| 守卫自激振荡 | 「页面不滚动就不限高」直接判 `scrollHeight ≤ clientHeight`：限高把 `scrollHeight` 压小了 → 下次判成「不溢出」→ 清限高 → 又溢出 …… 来回跳 | 改判「**解除限高后**会不会溢出」（`scrollHeight + (wrapper内容高 − wrapper当前高)`），与当前是否限高无关 |
| 下限写死 | 固定 `MIN=200px` 会在一部分页面上白白多留几十像素，把表头顶回筛选条底下（实测差 40px） | 下限改为实测的「表头高 + 1 行高」 |

### 2.16.6 GUI 实测

| 脚本 | 范围 | 结果 |
|---|---|---|
| `38_list_freeze.js` | 三端 × 全部导航项 × 全部子标签 × 每个 `dt-card`，1440×680 | **201 项通过 / 0 失败**（33 项跳过：空列表、短列表不构成滚动场景） |
| `39_list_freeze_mobile.js` | 移动端 390×844 | **15 项通过 / 0 失败** |
| `40_list_freeze_shots.js` | 桌面端截图留档 | 3 张 |

断言项：`F1` 列表上方无汇总标题（含整页兜底扫描）、`F2` 表头 `position:sticky`、
`F3b/F3c` 限高与「解除限高是否溢出」一致、`F4` 滚动列表内容时表头视口坐标不变、
`F5` 页面滚到底时表头未被筛选条遮挡、`F6` 分页条在滚动区之外且位置不随列表滚动变化。

截图归档：`gui-test-screenshots/{39a,39b,40a,40c}*.png` + 三个 `.log`。

### 2.16.7 已知边界

页面里列表之外还有别的内容时（多段式页面，如 `动物去向/回收` 的第一个列表后面
还排着「回收记录列表」），页面级滚动仍会把整个列表卡带出视口上方 ——
这是页面滚动而非「表头被筛选条压住」，实测中该场景按跳过处理并在日志里注明。
列表自身内部的冻结在所有页面上都成立。

### 2.16.8 本轮验证结果

- 全量测试 **589 通过**（模板与静态资源改动未影响任何断言）
- 静态资源版本号 `?v=20260918c` → `?v=20260918d`（5 个文件、6 处引用同步：
  `base.html` 的 CSS 与 `tnr-common.js` 各 1 处 + 4 个 portal 模板各 1 处 `tnr-api.js`）
- 顺带清理：`.std-card-actions` 已成死 CSS（只服务于刚移除的列表标题头），一并删除

---

## 二点十七、全列表搜索条件 / 分页 / 标题去括号 / 高度下限 10 行（2026-09-18 第十二轮）

### 2.17.1 需求

1. **没有搜索条件的列表补「常用搜索条件」** —— 全部补，跳过两张非列表
   （政府端看板的「区县业务统计」、系统配置的「编号规则配置」）。
2. **没有分页的列表补分页**。
3. **所有列表表头一律冻结**，含政府端手写的 `<table class="data-table">`。
4. **列表高度按默认 10 行设置** —— 原先下限是「表头高 + 1 行高」，
   一部分页面因此只剩 1~2 行，不符合日常操作习惯。
5. **列表卡片标题（含括号内容）全部去掉**，保留操作按钮；
   具体数量看列表下方的「共 N 条」。

### 2.17.2 需求 3 与需求 4「冲突」的真正根因

用户的原话是：

> 「为什么会冲突？列表应该在表头的下面，下面的位置不应该被控制死，可以根据行数自动增减的吧」

**用户是对的，两者本来不冲突。** 冲突来自实现：`position: sticky` 只能钉在
**最近的滚动容器**里，而 `.table-wrapper` 带着 `overflow-x: auto`
（CSS 规定一轴非 `visible` 时另一轴 `visible` 会计算成 `auto`），
于是它自己成了滚动容器 —— 而它的高度由内容撑开、永远不滚动，表头便彻底失效。

一旦给 wrapper 加上「10 行下限」的 `max-height`，wrapper 真的开始滚了，
表头就钉在 wrapper 顶端 —— 而 wrapper 顶端会被页面滚动带到筛选条底下，
看起来就是「表头被搜索条压住」。**两者的矛盾是 wrapper 同时充当滚动容器造成的**。

修法（沿用第十一轮的页面级 sticky 分支，只是把下限从 1 行提到 10 行）：

```js
const MIN = thH + rowH * 10;          // 表头高 + 10 行高
let cap = Math.max(MIN, Math.round(target));
if (cap >= natural) {
  // 内容放得下：**放开 wrapper 的纵向滚动**，让最近的滚动容器变成 .portal-content，
  // 表头钉到页面级 —— 于是「表头在筛选条下面」与「列表至少 10 行」可以同时成立。
  wrap.style.overflowX = 'clip';      // clip 不产生滚动容器（hidden 会）
  wrap.style.overflowY = 'visible';
} else {
  wrap.style.overflow = 'auto';
  wrap.style.maxHeight = cap + 'px';
}
```

「10 行下限」只在**内容确实超过 10 行**时才拉起滚动；不足 10 行时
`max-height` 只封顶不拉伸，列表仍按实际行数显示（用户说的「按行数自动增减」）。

### 2.17.3 补搜索条的范围

| 端 | 补的位置 | 数量 |
|---|---|---|
| 捕捉点 | 物料供应链（库存台账 / 采购入库 / 下发出库）、动物去向 4 个标签页（回收**两张表各一条**、放养两张表**共用一条**、领养、死亡）、领养审核管理、领养回访管控、转运交接「待转运」 | 12 |
| 医院 | 动物交接管理、诊疗记录、物料库存管理（4 标签页共用一条）、单据台账（4 标签页共用一条）、安乐死处置、领养信息维护 | 6 |
| 政府 | 物料全局监管 2 处 —— **两张手写 `<table class="data-table">` 一并改造成标准 `mountTable` 列表**（顺带删掉「物料流水追踪（最近 10 条）」的硬截断，改为全量 + 分页 + 日期筛选） | 2 |
| 领养人 | 移动端卡片布局、无表格，领养大厅已有搜索框 —— **经审计确认无需改造** | 0 |

同页有**两个**列表时各带一条筛选条，且**必须各自绑定自己的容器**：
`getFilterValues(scope)` 会遍历作用域内全部 `[data-filter]`，
两边 key 都叫 `keyword` 时会互相覆盖（「动物去向 / 回收」就是这种）。

### 2.17.4 本轮发现并修复的 3 个真实缺陷

补筛选条的过程中，用「筛选条真的能过滤吗」这条断言把三类**静默故障**逼了出来
（共同特征：界面不报错、下拉长得像模像样，只是筛不出东西）。

**缺陷 1（最严重）：政府端台账的区县筛选恒为空表 —— 拿 DOM 当数据源**

```js
// 修复前
const sel = document.querySelector('[data-filter="district"]');   // 全局裸查！
const districtName = sel?.selectedOptions[0]?.textContent || '';
records = records.filter(r => r.district_name && r.district_name === districtName);
```

两重错误叠加：
1. **全局裸查** —— 各 `.page-view` 全部常驻 DOM，`document.querySelector` 命中的是
   **文档里第一个**区县下拉（机构管理页那个），跟用户在台账页选的东西毫无关系；
2. **把 DOM 的显示文本当数据源** —— 那个下拉此时处于默认态，
   `textContent` 是占位项，于是过滤结果**恒为 0 条**。

实测证据（修复前后同一用例）：

| | 原=9，选各区县后的条数 |
|---|---|
| 修复前 | 选**任何**区县 → `0` |
| 修复后 | `襄城区=9 樊城区=0 东津新区=0 襄州区=0 南漳县=0` |

修法：用 `getFilterValues()` 给出的 district **id**，到缓存的区县表里换算名称
（台账行只有 `district_name`、没有 id），**完全不碰 DOM**。

**缺陷 2：领养审核的状态筛选与后端 choices 脱节（2 个死选项 + 漏 1 个真值）**

初版按「想当然的审批流」写成 `pending / pending_claim / completed / rejected`，
而 `Adoption.status` 的真实 choices 只有 `pending_claim / completed / cancelled`：

| 选项 | 判定 |
|---|---|
| `pending`（待审核） | ❌ 后端**不存在** → 选中永远 0 条 |
| `pending_claim` | ✅ |
| `completed` | ✅ |
| `rejected`（已驳回） | ❌ 后端**不存在** |
| `cancelled`（已取消） | ⚠️ **漏了** —— 真实存在却选不到 |

**缺陷 3：捕捉管理状态筛选漏 `void`（已作废）** —— 作废单照样占着列表行，
却无法把它们筛出来。

**附带修复：区县下拉里的「全市（市级）」死选项（4 处）**
业务记录归属区县一律落到具体区县（`_resolve_district_scope` 明确拒绝市级），
所以「全市（市级）」永远筛不出任何一行；而它排在下拉第一位，
用户很容易把它当成「全部」，选完看到空表会以为数据丢了。
统一改为 `.filter(d => !d.is_city)`（区县管理页早已是这个写法）。

### 2.17.5 新增 6 个静态回归测试

`core/tests_frontend_consistency.py` 新增三个类，把这轮踩到的两类静默故障锁死：

| 类 / 用例 | 锁定的缺陷 |
|---|---|
| `FilterBarOptionCoverageTest`（4 个） | 下拉筛选选项与后端 choices **双向**对齐：**缺项**（用户筛不到）与**多项**（死选项）都断言 |
| `DistrictFilterExcludesCityTest` | 凡「取值来自区县接口」的下拉，必须过滤市级 |
| `FilterValueSourceTest` | 模板中不得出现 `document.querySelector('[data-filter…')` 全局裸查，也不得用 `selectedOptions` 读下拉文本当数据源 |

配套解析器 `select_filters()`：扫出源码里全部 `type: 'select'` 的筛选条
（key / label / 字面量取值集合 / options 表达式 / 偏移量）。
同一 portal 里 label 为「状态」的筛选条有四五条，靠 key+label 无法区分，
因此用「某个只有该实体才有的枚举值」做指纹定位（如 `partial` 只属于 `Capture`）。

**已验证测试真的能咬住**：临时把领养选项改回错误版本后，
`test_shelter_adoption_status_filter_covers_all` 立即变红并指出
`缺少 ['cancelled']`，恢复后转绿。

### 2.17.6 GUI 实测

| 脚本 | 范围 | 结果 |
|---|---|---|
| `46_pages_smoke_audit.js` | 四端 × 全部导航项 × **全部子标签**，80 个视图 | **0 报错 / 0 空白** |
| `47_filter_works.js` | 三端 23 条代表性筛选条 | **23 通过 / 0 失败 / 0 跳过** |
| `41_all_tables_audit.js` | 三端全部列表 | **32 张表全部 sticky / 真实遮挡 0** |
| `42_lists_inventory.js` | 列表高度下限 | 10 行下限生效 |

`47_filter_works.js` 的下拉类断言本轮**从「只试第一个选项」改为「遍历全部选项」**：

- 原断言 `after >= 1` 会把「该取值在当前数据里恰好 0 条」误判成失败
  （如还没有「待审核」的领养单）；
- 新断言看整组选项的条数分布，要求 `max >= 1`（能筛出数据）
  且 `min < 原值`（过滤确实生效）—— 两条分别对应
  「选什么都空」（缺陷 1）与「选什么都不变」两类故障。

实测输出的选项分布（可见修复后的数据分布是合理的）：

```
捕捉管理 状态   [pending=3 partial=3 completed=6 void=0]        原=12
转运明细 状态   [pending=1 received=4 rejected=1 void=2]        原=8
流向查询 类型   [purchase=17 dispatch=20 receive=20 consume=66 adjustment=0]  原=123
领养审核 状态   [pending_claim=2 completed=1 cancelled=1]       原=4
捕捉台账 区县   [襄城区=6 樊城区=3 东津新区=1 襄州区=2 南漳县=0]   原=12
gov 诊疗台账 区县 [2=9 3=0 4=0 5=0 7=0]                         原=9
```

### 2.17.7 去标题清单

**去掉**（列表卡）：捕捉点端「领养记录」「月度打卡记录」「待放养确认」「已放养记录」、
全量台账各标签页；医院端「异动记录」「安乐死处置记录」；
政府端「编号规则配置」「区县业务统计（区间）」「物料流转统计（区间）」
「业务动态（区间 · 最近 N 条）」「物料库存明细」「物料流水追踪（最近 10 条）」。

**保留**（表单卡，标题是操作对象而非列表汇总）：领养登记 / 采购入库登记 /
下发出库登记 / 发起放养 / 新建转运单 / 违规收回操作 / 库存异动报备 /
新增安乐死处置。

### 2.17.8 本轮验证结果

- 全量测试 **595 通过**（第十一轮 589 + 本轮新增 6 个静态回归用例）
- 静态资源版本号 `?v=20260918h` → `?v=20260918i`
  （5 个文件、6 处引用同步：`base.html` 的 CSS 与 `tnr-common.js` 各 1 处
  + 4 个 portal 模板各 1 处 `tnr-api.js`）
- 改完模板必须**重启服务**才能生效（Django 5.0 的 `cached.Loader` 只靠进程重启失效）

### 2.18 第十三轮：修掉「10 行下限」估算缺陷 + 补齐盘点脚本的覆盖盲区

第十二轮的「列表高度按默认 10 行」有一个**静默少一行**的缺陷，本轮定位并修复。

#### 2.18.1 缺陷：行高不齐时，需要限高的表格会少显示一行

`TNR_UI._fitTableHeight()` 的下限原为「表头高 + **首行行高** × 10」。表格里长文本会折行，
**同一张表的行高并不均匀**，用首行估算就会少算几十像素。

实测 `[捕捉点] 全量台账中心 › 捕捉台账`：

| 项 | 值 |
|---|---|
| 首行行高 / 折行行高 | 69.78px / **76.19px**（第 5、7、10 行） |
| 按首行估算的 10 行高 | 742px（`maxHeight` 设成 744，含 2px 边框） |
| **实际**需要的高度 | **767px** |
| 结果 | 第 10 行被裁掉，**只完整显示 9 行**；多出一条 25px 的内部滚动条 |

界面**完全不报错**，表格照常渲染，只有量「可视行数」才发现得了。

#### 2.18.2 修复

- 下限改为**实测前 N 行的累计高度**：第 N 行的 `getBoundingClientRect().bottom`
  减去 `tbody` 的 top，不再用「首行行高 × N」估算。
- 下限的行数是**固定 10**（常量 `FLOOR_ROWS`），**不随分页的「条/页」控件缩放**
  （见 2.18.6 —— 这是同轮自查发现的第二个坑）。
- 限高值补上 wrapper 自身的**上下边框与内边距**：`max-height` 作用在**边框盒**上
  （实测 `maxHeight=744` → `clientHeight=742`），少算这 2px 同样会裁掉最后一行。
- 「自然高」与「限高值」**统一到边框盒单位**（`wrap.scrollHeight + boxY`），
  否则「只差 1~2px 就取自然高」的容差判断会失真。

修复后同一张表：`maxHeight=—`（走页面级冻结分支）、内部可滚 **0px**、10 行全部可见。

#### 2.18.3 新增回归守卫（3 例，已做变异验证）

`core/tests_frontend_consistency.py::TableHeightFloorTest`：

| 用例 | 断言 |
|---|---|
| `test_floor_is_measured_not_estimated` | 方法体里**不得**出现 `MIN = ... rowH * <数字>`；且必须量了行下沿 |
| `test_floor_is_fixed_ten_rows` | 下限**不得**读 `.dt-pagesize`；`FLOOR_ROWS` 常量必须是 **10** |
| `test_floor_covers_wrapper_border` | 必须把 wrapper 上下边框计入 |

**变异验证**（三个方向都咬得住）：

- 把下限改回 `thH + rowH * 10 + boxY` → 失败并**精确指向 `tnr-common.js:494`**；
- 去掉行下沿测量（改成 `trows.length * 44`）→ 失败并指出「必须实测前 N 行累计高度」；
- 把 `FLOOR_ROWS` 改成 `20` → 失败（`'20' != '10'`）；
  在下限里加回 `el.querySelector('.dt-pagesize')` → 失败并指出「下限不得随每页条数变大」。

> 写这两个断言时踩到两个坑，都已修掉并写进 skill：
> ① `getBoundingClientRect().bottom` 在方法里还出现在卡片下沿 `cardBottom` 那行，
> 只匹配它会被**无关代码满足**（变异测试才发现咬不住）——要收紧成「`.bottom` 减去某元素的 `.top`」；
> ② 从**方法体**里取到的匹配偏移是相对方法体的，直接拿去索引整份源码会算出离谱行号
> （494 行报成 76 行），必须加上方法体在源码中的起始下标。

#### 2.18.4 盘点脚本的两处覆盖盲区（`gui-test-scripts/`，本地工具）

- **标签页内的表格几乎全被漏掉**：选择器原为 `.tabs .tab-item[data-tab]`，
  而只有捕捉点端的标签带 `data-tab`（医院端 `data-rtab`/`data-mtab`、政府端 `data-ledger-tab`）。
  改为通用的 `.tabs > .tab-item, .sub-tabs > .sub-tab` 后，盘点到的列表数 **32 → 50**。
- **只认 `table.data-table`，看不见非表格形态的列表**。补上卡片网格（`.stat-grid > .card`）
  与日志条目（`.log-entry`）的盘点后，识别出 2 个非表格列表：
  医院端「实时库存」（卡片网格）、政府端「操作日志」（日志列表）。

#### 2.18.5 收紧判据后复跑（第十三轮结果）

| 脚本 | 结果 |
|---|---|
| 全量测试 | **598 通过**（595 + 本轮新增 3 例） |
| `42_lists_inventory.js` | 共 **50** 个列表；**被压扁 0 个**；无搜索条 1 个、无分页 2 个 —— 全是用户明示跳过的两张非列表 |
| `52_probe_capped_table.js`（新增） | 修复前「限高 744 / 内容 767 / 裁 1 行 / 内部可滚 25px」→ 修复后「不限高 / 10 行可见 / 内部可滚 0px」 |

> 判据原为 `visibleRows < min(rows, 10) - 1`，那个 **1 行容差恰好把这个缺陷放了过去**。
> 收紧成 `visibleRows < min(rows, 每页条数)` 后复跑仍为「被压扁 0 个」——
> 同时证明修复有效、且没有别的列表被带坏。
>
> 静态资源版本号 `?v=20260918i` → `?v=20260918j`（同样 5 个文件、6 处引用）。

#### 2.18.6 同轮自查发现的第二个坑：下限行数不能随「条/页」缩放

第一版修复把下限的行数写成了「取当前每页条数」（`el.querySelector('.dt-pagesize')`），
当时还以为这是「更通用」的写法。跑 `54_probe_pagesize_floor.js` 才看清是错的
（用例：医院端「物料库存管理 › 出入库台账」，共 23 条记录）：

| 条/页 | 本页行数 | 可视行数 | `maxH` | 容器高 | 内部可滚 |
|---|---|---|---|---|---|
| 10 | 10 | 10 | `—` | 531px | 0px |
| 20 | 20 | **20** | `—` | **1017px** | 0px |
| 50 | 23 | **23** | `—` | **1163px** | 0px |

`maxH` 全为 `—` —— **下限完全没生效**。原因很反直觉：
**一页渲染的行数 = 每页条数**，所以「自然高」恒等于「一页的行高」，
`MIN ≈ natural` ⇒ `cap >= natural - 2` 的容差**永远成立** ⇒ 判成「内容放得下」
⇒ 走「页面级」分支、压根不限高。20 条/页 时列表长到 1017px，比 900 的视口还高。
更糟的是：行数 > 50 的表在 50 条/页 下，下限会算出 50 行（约 2500px），列表被顶出屏幕。

改成固定 `FLOOR_ROWS = 10` 后：

| 条/页 | 本页行数 | 可视行数 | `maxH` | 内部可滚 |
|---|---|---|---|---|
| 10 | 10 | 10 | `—`（页面级分支） | 0px |
| 20 | 20 | 11 | **597px** | 420px |
| 50 | 23 | 11 | **604px** | 559px |

（597px > 10 行所需的 530px ⇒ 视口允许多少就多少，**下限作为最小值被满足**，正是设计意图。）

**结论：「表格总行数 > 下限行数」才是下限真正起作用的路径。** 默认 10 条/页时，
只要表格一页放得下，容差就会把它送去「页面级」分支、`maxH` 恒为 `—` ——
实测 50 张表在 **900 与 680 两个视口下全部** `maxH=—`。
**只跑默认条/页的盘点，验证不到限高分支**，必须把「条/页」调大再测一遍。

顺带修掉一个我自己写错的断言：第一版把 `test_floor_follows_page_size_control`
（断言「行数必须取自 `.dt-pagesize`」）加了进去 —— 那是**把错误行为固化成了守卫**，
已替换为 `test_floor_is_fixed_ten_rows`。

> 两条教训都写进了 skill 与项目记忆：
> ① 「通用化」的直觉要先用探针验证，别把没验证过的推断当成正确实现；
> ② **断言本身也要被验证** —— 一个写反的守卫比没有守卫更糟，它会让错误看起来是受保护的。

---

## 二点十九、政府端操作日志（审计）落地（2026-09-18 第十四轮）

### 2.19.1 问题：这个页面从上线起就永远是空的

政府端「系统配置 › 操作日志」读的是 **Django admin 的 `LogEntry`**，而业务接口
**从不经过 admin** —— 实测库里 `LogEntry` 0 行、业务记录 53 条。三个缺陷叠在一起：

| # | 缺陷 | 表现 |
|---|---|---|
| 1 | 数据源根本没有写入方 | 页面永远显示「暂无操作日志」 |
| 2 | `LogEntry` **没有 `district` 字段**，区县隔离只能退化成「按操作人的区县过滤」 | 即使有数据，挂在「全市（市级）」下的捕捉点操作员产生的日志，**本区县政府也看不到** |
| 3 | 前端搜索按 `l.action` 过滤，后端**从未返回过 `action`** | 搜索框提示写着「操作/详情/操作人」，但按「操作」搜永远是空结果，且不报错 |

第 3 条是典型的**跨层契约脱节**：字段名对不上时，前端既不报错也不提示，
只是安静地返回 0 条 —— 与「本来就没有这类日志」完全无法区分。

### 2.19.2 为什么不用 admin `LogEntry`（而不是"顺手复用"）

`LogEntry` 的 `content_type` 可空、`action_flag` 取值 `(1 新增/2 修改/3 删除)`
正好能对上前端已有的映射表，**看起来可以零迁移复用**。但第 2 条缺陷是结构性的：
`LogEntry` 没有区县字段，只能按 `user__district_id` 过滤，而这恰好是本项目
**已经踩过两次**的错误口径（捕捉单、黑名单都曾因此把本区数据落到市级）。
所以新建 `core.AuditLog`，显式记录**业务记录的归属区县**。

### 2.19.3 实现

| 层 | 文件 | 说明 |
|---|---|---|
| 模型 | `core/models.py` → `AuditLog` | `district` FK(`SET_NULL`) + `district_name`/`user_name`/`role` **写入时快照**；`module`/`action_flag`/`object_type`/`object_id`/`object_repr`/`summary`/`detail`/`success`/`ip`/`path` |
| 服务 | `core/audit.py` | `describe()`（按 URL 推导模块与动作）、`sniff_district_id()`/`sniff_object_repr()`（在嵌套响应体里找业务标识）、`resolve_district()`、`record()`、`write_log()` |
| 中间件 | `core/middleware.py` → `AuditLogMiddleware` | 记录 `/api/` 下**所有**写请求，白名单排除只读 POST（黑名单校验 / 编号预览 / 逆地理编码） |
| 接口 | `supervision/views.py` → `operation_logs` | 改读 `AuditLog`，支持 `module` / `action_flag` / `success` / `q` 筛选，手工聚合补驼峰 |
| 前端 | `templates/portal/gov/portal.html` | 渲染模块名 + 动作 + 对象标识 + 操作人 + 区县；搜索改按接口**真实返回**的字段 |

**归属区县解析顺序**：目标对象 → 视图显式指定 → 响应体 → 请求体 → 操作员区县。
「视图显式指定」排在请求体之前，因为请求体里的 `district_id` 是前端可伪造的；
响应体排在请求体之前同理 —— 它是服务端落库后的真实值。

**用中间件而不是在每个视图手写一行**：业务写接口近 40 个，逐个手写必然漏，
而漏掉的接口在日志页上的表现是「这个操作没发生过」，比没有日志更危险。

### 2.19.4 过程中发现并修掉的 4 个坑（都是自己写出来的）

| 坑 | 现象 | 修法 |
|---|---|---|
| **`audit_skip` 判早了** | 视图里设的 `request.audit_skip = True` 完全不生效（消息已读回执照样入库） | `_should_log()` 在**请求进入时**判定，那时视图还没跑；必须在**视图执行后**的 `_record()` 里再判一次 |
| **把日志归属到正在删除的区县** | 删区县时 `AuditLog.district` 指向一个马上就不存在的行 → 外键报错 → **整条日志被吞掉**，最该留痕的操作反而没记录 | 区县删除只写摘要、不写 FK（`district` 留空 = 仅市级可见），`district_name` 快照保留名称 |
| **响应体里读不出区县** | `CheckIn` / `AdoptionHallListing` / `AdoptionApplication` 本身没有 `district` 字段；`capture_delete` 只回 `id/is_deleted`；机构启停只回 `id/status` | 这些视图补一行 `request.audit_district = ...`（共 12 处） |
| **详情列内容重复** | 摘要已拼成「模块·动作 对象标识」，前端又拼一遍 `objectRepr` → 「审计演示123 黑名单·新增 审计演示123」 | 摘要里已包含对象标识时不再补 |

### 2.19.5 新增 45 个测试

`core/tests_audit.py`（45 个）+ 改写 `supervision/tests.py` 里 1 个把缺陷固化了的用例。

**其中最重要的两类：**

1. `AuditWriteCoverageTest` —— 逐个写接口验证「留下了记录 **且归属区县正确**」。
   最容易漏的形态不是「没写记录」，而是**写了但 `district` 为空**：
   市级管理员看得到、区县政府看不到，该区县的监管等于瞎的。
2. `AuditCityOperatorAttributionTest` —— 用**挂在「全市（市级）」的操作员**跑同样的流程。

第 2 类是必需的，这一点是**变异验证逼出来的**：把 `sniff_district_id()` 改成恒返回
`None`（模拟"退化成按操作人归属"）后，用区县操作员的用例**全绿** —— 因为操作员的
区县恰好等于业务记录的区县，实现错了也测不出来；只有市级操作员那批会红。
**用例的夹具配置本身就是断言强度的一部分。**

顺带修掉一个把缺陷固化了的旧断言：`supervision/tests.py::test_operation_logs`
原先用 `LogEntry.objects.log_action()` 手工塞一条 admin 日志再断言接口返回它 ——
等于**把「日志页永远为空」这个缺陷写成了守卫**。

### 2.19.6 GUI 实测（`gui-test-scripts/55_audit_logs.js`，11 项检查 0 失败）

```
[WRITE] cy_shelter 新增黑名单「审计演示861173」→ {"success":true}
[LOGS]  襄城区政府 操作日志 3 条
[ROW]   2026-09-18 16:57 | [黑名单] 新增 | 黑名单·新增 审计演示861173 | 襄城捕捉点操作员 · 襄城区
[PASS]  操作日志不再为空（旧实现恒为 0 条）
[PASS]  搜索「新增」命中 3 条（旧实现按 l.action 过滤 → 恒为 0）
[PASS]  搜索操作人「cy_shelter」命中 3 条
[PASS]  搜索不存在的关键词返回 0 条（搜索确实在过滤，而不是恒真）
[LOGS]  樊城区政府 操作日志 0 条
[PASS]  樊城区政府看不到襄城区的操作（区县隔离生效）
```

截图：`r14_gov_logs.png` / `r14_gov_logs_search.png` / `r14_gov_logs_other_district.png`。

**注意 `cy_shelter` 的区县是「全市（市级）」**（与现场一致），而日志归属的是
**襄城区** —— 这正是本轮修复的核心口径，也是襄城区政府能看到这条记录的原因。

> 顺带把「站外字体挂死截图」的拦截**下沉到 `gui-test-scripts/lib.js` 的 `session()`**：
> 离线沙箱下 `page.screenshot()` 会先等 `document.fonts.ready`，字体永远加载不完，
> 截图就永远卡住（第十二轮为此白排查过一轮）。现在所有脚本自动受益，不必各写一遍。

### 2.19.7 本轮验证结果

| 项 | 结果 |
|---|---|
| 全量测试 | **643 通过**（598 → +45） |
| `46` 全端冒烟 | **80 个视图 0 报错 0 空白** |
| `47` 筛选条守卫 | **23/23 通过** |
| `55` 操作日志实测 | **11/11 通过**（含区县隔离与搜索） |
| 变异验证 ① | `sniff_district_id` 恒返回 `None` → **6 个用例变红**（含 4 个市级操作员用例） |
| 变异验证 ② | 去掉日志接口的区县过滤 → **3 个用例变红** |
| 变异验证 ③ | 去掉只读 POST 白名单 → **1 个用例变红** |

### 2.19.8 已知边界

- **历史操作无法追溯**：`AuditLog` 是新建的表，只有本轮上线之后的操作才有记录。
  此前发生的 53 条业务记录没有对应日志，也不应该伪造。
- **`district` 为空的日志仅市级可见**：包括系统参数修改、区县删除这类
  「不属于任何具体区县」的操作。这是有意为之 —— 让它们对区县可见等于绕开区县隔离。
- **消息已读回执不写审计**（`request.audit_skip`）：它没有归属区县，且每次打开消息
  都会产生一条，只会把真正有意义的业务操作挤出日志页。

---

## 二点二十、账号区县 ↔ 机构区县一致性（2026-09-18 第十五轮）

### 2.20.1 问题：两个「谁也没拦住」的入口

上一轮（2.19）顺路查到一处不一致：`nzzs` 账号区县 = **南漳县**，
所属机构「宠安宠物诊所」在**东津新区**。查根因时发现是两个入口都没拦：

| 入口 | 缺的校验 | 后果 |
|---|---|---|
| `supervision.user_create` | 只校验「医院必须选具体区县」，**不校验与所属机构一致** | 界面上的「所属区县」与「关联机构」是两个独立下拉，选完谁也不知道它们矛盾 |
| `supervision.institution_edit` | 改区县时**不联动**挂靠的操作员 | 市级管理员把机构搬到别的区县，操作员被静默留在旧区县 |

两者叠加的结果是同一个：这个账号**看到 A 区县的档案、却以 B 区县的机构身份写数据**。

关键在于**任何一侧单看都合法**：账号区县是个有效区县，机构也是个有效机构，
列表里两列都正常显示 —— 只有把两列并排看才看得出来。实测库 9 个演示账号里
只有 1 个踩中，正是最难被发现的那一类。

而且政府端「用户管理」**只有新增与启停、没有编辑**，界面上根本改不回来。
这一点决定了本轮不能只做到「报错」为止。

### 2.20.2 为什么这条不一致特别危险

- `user.district` 决定**读取**侧的区县隔离范围（`get_district_filtered_queryset`）；
- `user.institution` 决定它**写**数据时代表谁（`Transfer.from_shelter`、
  `Treatment.hospital` 等全部由它推导）。

两者不一致 = **读的是 A 区、写的是 B 区**，而写入的数据会带着 B 区的归属落库 ——
既污染 B 区的统计，又让 A 区的监管看不到这批操作。这与 2.10.3（捕捉单区县误标）、
2.19（操作日志按操作人归属）是同一个错误口径的第三次出现：
**归属要从业务对象推导，不能从操作者推导。**

### 2.20.3 实现：四层，缺一层就漏

| 层 | 位置 | 作用 |
|---|---|---|
| **拦** | `services.validate_operator_district()` + `user_create` | 建号时挡下来，提示里同时点明两侧区县名 |
| **搬** | `services.cascade_operator_district()` + `institution_edit` | 换区县时联动，与机构写入**同一事务** |
| **巡** | `manage.py check_data_integrity` | 只读巡检 13 类问题，有违规退出码 1 |
| **修** | `manage.py sync_operator_district` | 默认预演，`--apply` 才写库 |

判定规则：

- **医院操作员**：账号区县必须**等于**机构所在区县（医院不能挂市级）。
- **捕捉点操作员**：允许挂「市级」—— 现场约定，两个捕捉点操作员都挂在
  「全市（市级）」下（与 `resolve_district_scope` 的口径一致）；
  但若挂了具体区县，就必须与机构一致。
- **级联只搬「区县恰好等于机构原区县」的账号**：挂市级的捕捉点操作员**不动**。

**为什么不把修复塞进 `check_data_integrity`**：那条命令的价值就在于
**可以在任何环境放心跑**（生产、只读副本、排查现场）。一旦它可能写库，
这个价值就没了。所以拆成两条：一条永远只读，一条默认预演。
修复方向统一以**机构**为准 —— 机构是有实体的（医院就在那个区县），
而账号的 `district` 只是建号时填的一个字段。

`DEPLOY.md` 相应补了「6.1 数据一致性巡检」与自检清单一行。

### 2.20.4 过程中自己写出来又修掉的 3 个坑

1. **巡检命令把「校验通过」当成了问题条目**：`validate_operator_district()` 通过时
   返回 `None`，第一版直接把返回值当明细打印 —— 实测 **9 个正常账号全被误报**成
   「不一致」。现在有专门的误报守卫用例（`test_consistent_accounts_are_not_reported`）。
   **「报了没人信」和「不报」一样糟** —— 巡检一旦开始误报，下一步就是没人再看它。
2. **测试夹具本身就是断言强度的一部分（又一次）**：`make_institution()` 从不写 `code`，
   而生产库里每个机构都有编号（`seed_data` 按 code 幂等匹配、`institution_create`
   也会自动生成）。于是 5 个正常夹具机构被巡检报成「机构缺少业务编号」。
   这不是巡检误报 —— 是**夹具跑在了一个现实中不该存在的数据形态上**。
   修法是让夹具默认带编号（`TI###` 前缀，与生产 `I###`/`C###` 区分，不干扰取号断言）。
   同类教训在 2.19 已经出现过一次（那次的夹具是「操作员挂在市级」）。
3. **GUI 用例自己写窄了**：还原步骤断言「1 个操作员」，但此时该医院下已经有 2 个
   （原操作员 + 本次新建的探针账号）→ **正确行为被判成失败**。
   改成按实际挂靠数断言，顺带证明了**级联搬的是全部挂靠账号**，不是只搬第一个。

### 2.20.5 新增 29 个测试

| 测试类 | 数量 | 覆盖 |
|---|---|---|
| `ValidateOperatorDistrictTest` | 7 | 医院/捕捉点/挂市级/缺区县/无机构 |
| `ResolveOperatorDistrictTest` | 1 | **互补不变式**（表驱动 10 组） |
| `CascadeOperatorDistrictTest` | 3 | 搬走 / 不动市级 / 区县未变时空转 |
| `CheckDataIntegrityCommandTest` | 9 | 基线、**误报守卫**、明细截断、只读、闭环 |
| `SyncOperatorDistrictCommandTest` | 5 | 预演不写库、`--apply` 写库、市级不动、`--username` 限定、修完巡检闭环 |
| `InstitutionApiTest` / `UserApiTest` | 4 | 接口层级联与拦截 |

其中 `ResolveOperatorDistrictTest` 是本轮最重要的一条：校验负责**拦**（建号时）、
解析负责**修**（历史账号），两者一旦对同一输入给出矛盾结论，就会出现
「巡检报错、修复脚本却说不用改」的死循环 —— 而**两边各自的用例都会通过**，
因为没人测过它们的一致性。所以用一张表把两个结论并排断言。

### 2.20.6 变异验证

| 变异 | 变红的用例数 |
|---|---|
| ① `validate_operator_district` 恒返回「通过」 | **5**（服务层 2 + API 2 + 巡检 1） |
| ② `cascade_operator_district` 直接返回 0（不搬人） | **2** |
| ③ 巡检退回「不判 `None`」的写法 | **2**（正是那两条误报守卫） |
| ④a `resolve_operator_district` 恒返回「无需修复」 | **8** |
| ④b 解析方向反了（以账号为准，而不是以机构为准） | **8** |

④a / ④b 让同样 8 个用例变红，说明用例同时锁住了「**要不要修**」与「**往哪修**」，
而不是只锁住其中一个。

### 2.20.7 GUI 实测（`gui-test-scripts/56_operator_district.js`，15 项检查 0 失败）

用真实演示数据来回搬（襄州区惠民动物医院 ←→ 东津新区），跑完自动还原：

- 市级管理员改机构区县 → 提示 `机构更新成功，1 个操作员的所属区县已同步调整`，
  且操作员确实跟着搬（旧实现只弹「机构更新成功」，操作员静默留在旧区县）；
- 账号区县 ≠ 机构区县建号 → 被拦下，提示 `账号所属区县（襄州区）与机构所在区县
  （东津新区）不一致，请改为一致`，且**没有落库**（不是「先建后报错」）；
- 区县一致 → 正常创建（校验没有误伤）；
- 搬回原区县 → 报 `2 个操作员`（含本次新建的探针账号），证明搬的是**全部**挂靠账号；
- 结束后演示数据回到初始状态。

### 2.20.8 真实库处理

`sync_operator_district` 预演 → 备份 `db.sqlite3.bak-20260918-171318-before-nzzs-fix`
→ `--apply`，把 `nzzs` 从「南漳县」改到「东津新区」→ 复跑 `check_data_integrity`：
**未发现一致性问题**（退出码 0）。

### 2.20.9 本轮验证结果

| 项 | 结果 |
|---|---|
| 全量测试 | **672 通过**（643 → +29） |
| `46` 全端冒烟 | **80 个视图 0 报错 0 空白** |
| `47` 筛选条守卫 | **23/23 通过** |
| `56` 账号区县一致性实测 | **15/15 通过** |
| `check_data_integrity`（真实库） | **未发现一致性问题**（退出码 0） |
| 变异验证 ①/②/③ | 5 / 2 / 2 |
| 变异验证 ④a/④b | 8 / 8 |

### 2.20.10 已知边界

- **巡检只报不修**；`sync_operator_district` 只修「账号↔机构」这一类 ——
  其余 12 类的正确修法需要人工判断（例如「宠物未作废但捕捉单已作废」要先确认
  是补作废还是恢复捕捉单）。
- **修复方向以机构为准**：若实际情况是**机构标错了区县**，应该改机构而不是改账号。
- **政府端用户管理仍没有编辑入口**：改账号区县目前只能靠 `sync_operator_district`
  或 Django admin。补一个用户编辑弹窗是下一轮值得做的事。
- **南漳县及其 2 家机构只存在于当前库**：`seed_data.py` 只定义
  D001–D004 / I001–I006 / C001–C004，**全新部署不会有南漳县**，`nzzs` 也无法复现。
  按「区县以后会全部清除重建」的既定说法，这可能是过渡状态 —— 所以**未改脚本**。

---

## 二点二十一、区级管理员可越权建出市级管理员（2026-09-18 第十五轮续）

### 2.21.1 怎么发现的

给「账号区县不一致」补服务端校验时，顺手检查 `user_create` 里还有没有别的
「前端做了、后端没做」的校验 —— 一眼看到 `GovPortal.canCreateRole()`：
它按操作员角色过滤了角色下拉的选项，而 `user_create` 里**没有任何对应判断**。

### 2.21.2 实测（区级管理员账号，直接打接口）

| 请求 | 结果 |
|---|---|
| `role=gov_city` + 市级 `district_id` | **200 用户创建成功** |
| `role=gov_district` + 本区 `district_id` | **200 用户创建成功** |
| `role=shelter` | 400（被「必须关联捕捉点机构」挡下，与越权无关） |

拿到口令登录后 `get_district_scope()` 返回 `None` → **全部区县的数据都看得到**，
还能管机构、管账号、管区县。这是一条完整的提权链。

同时 `user_create` 也**不校验 `district_id` 是否落在自己的管辖范围**，
所以区级管理员还能把账号建到别的区县 —— 另一条跨区县后门。

### 2.21.3 为什么这类漏洞最难被发现

界面上**根本点不出这个选项**（下拉里就没有「市级管理员」），所以无论怎么点都正常；
只有直接打接口才会暴露。

> **「前端把选项过滤掉了」不是校验，只是没把错误的选项摆出来。**

这与第五节（前后端契约一致性）是同一件事的两面：
那一节讲的是「后端是对的、前端没用对」，这次是「前端拦住了、后端根本没拦」——
**后者更危险，因为前者在界面上看得见，后者看不见。**

### 2.21.4 修法

- `services.MANAGEABLE_ROLES`（服务端**唯一真源**）+ `validate_user_manage_scope()`：
  1. 目标角色必须在他可管理的集合里（`gov_district` → 只能 `shelter`/`hospital`）；
  2. 区级管理员只能管「**账号所代表的对象**落在本区县」的账号。
- 第 2 条的判据是「代表谁」，**不是账号自己的 `district`**：
  捕捉点操作员按现场约定挂在「全市（市级）」（见 `resolve_district_scope`），
  判账号区县会把区级管理员**唯一能建的那类账号全挡掉**。所以捕捉点与医院
  都判**机构所在区县**。
- 前端把区级管理员的「所属区县」下拉收敛到本区县：原先列全部 4 个区县，
  而「关联机构」只列本区县的 → 随手选一个他区就必然撞上「不一致」，白填一遍。

### 2.21.5 新增 13 个测试

| 测试类 | 数量 | 覆盖 |
|---|---|---|
| `ValidateUserManageScopeTest` | 7 | 角色白名单、跨区县、**正向对照**、非政府角色 |
| `UserApiTest` 新增 | 5 | 越权建号被拒且不落库、跨区县被拒、**正向对照**、市级不受限 |
| `ManageableRolesContractTest` | 2 | **把前后端两侧的角色集合绑死** |

**正向对照是必须的**：只加拦截不测放行，过紧的校验发现不了 ——
本轮最容易写错的就是「捕捉点操作员判账号区县」，那样会把区级管理员
唯一能建的那类账号全部挡掉，而只测拦截的用例会全绿。

`ManageableRolesContractTest` 直接读 gov 模板里的 `canCreateRole()` 源码，
与 `MANAGEABLE_ROLES` 做集合比对：**改一侧不改另一侧就红**。

### 2.21.6 变异验证

| 变异 | 变红的用例数 |
|---|---|
| 后端 `gov_district` 可建角色里加上 `gov_district` | **4**（服务层 1 + API 2 + 契约 1） |

### 2.21.7 本轮验证结果

| 项 | 结果 |
|---|---|
| 全量测试 | **685 通过**（672 → +13） |
| `46` 全端冒烟 | **80 个视图 0 报错 0 空白** |
| `47` 筛选条守卫 | **23/23 通过** |
| `56` 账号区县一致性实测 | **22/22 通过**（新增区级管理员管辖范围 5 项） |
| 变异验证 | 前后端角色集合脱节 → **4 个用例变红** |

### 2.21.8 已知边界

- 这次只堵了 `user_create`。**`user_edit` 目前还不存在** ——
  政府端用户管理仍然只有新增与启停，账号区县填错只能靠
  `sync_operator_district` 或 Django admin 改。补编辑入口时
  **必须复用同一个 `validate_user_manage_scope()`**，
  否则会开出另一条同样的越权路径（例如改自己的 `role` 提权）。
- **同类「前端做了、后端没做」的校验值得再扫一遍**：凡是前端出现
  `canXxx()` / 下拉过滤 / `disabled` 的地方，都要问一句「接口自己拦不拦」。
  本轮只发现 `canCreateRole()` 一处（全项目 `can[A-Z]` 只有一个），
  但 `disabled` 与「选项里没有」这两种形态还没有系统扫过。

---

## 二点二十二、孪生接口的校验漂移 · 三条越权/越界路径（2026-09-18 第十六轮）

上一节（2.21）结尾留了一句：「同类『前端做了、后端没做』的校验值得再扫一遍」。
本轮按这句话往下扫，**扫出了一个跨区县读 PII 的洞、一条数据编辑提权链，
以及一处静默改判机构区县的静默数据损坏** —— 三条都不是同一个接口，
但根因是同一种写法。

### 2.22.1 本轮的方法：把「孪生接口」当成一个检查项

上一轮那条 `canCreateRole()` 的教训是「前端过滤 ≠ 校验」。本轮换个角度找：
**一对功能对称的接口，如果校验只写在其中一个里，那另一个就是洞。**
按这个思路逐个比对，命中三处：

| # | 孪生对 | 写在谁那里 | 漏在谁那里 | 后果 |
|---|---|---|---|---|
| 1 | `capture_update` / **`capture_detail`** | update 走 `_get_capture_for_user()` | detail 裸查 `Capture.objects.get(id=pk)` | **跨区县读个人信息** |
| 2 | **`institution_create`** / `institution_edit` | edit 有「不得调整到他区」 | create 没有区县范围校验 | 区级管理员在**他区**建机构 |
| 3 | `institution_create` / **`institution_edit`** | create 有「医院不能挂市级」 | edit 没有 | **把医院改挂市级 → 操作员获得全量可见** |

第 1、2 条是「同一对接口、校验只写一边」；第 3 条是反向的 ——
两边都有校验，但**内容不一样**。三种形态的根因相同：**校验各写一份**。

### 2.22.2 缺陷一：捕捉单详情跨区县泄漏（本轮最严重）

`capture_detail` 是裸查：

```python
capture = Capture.objects.get(id=pk)   # ← 不做任何范围校验
```

而这个接口返回的是**整张捕捉登记表**：物业交接人、联系电话、定位地址、经纬度、
整体合影、单只照片、**电子签名**、以及其下全部宠物档案。

实测（甲区账号读乙区捕捉单，`id` 直接猜）：

```
>>> shelter_a    status=200  泄漏! phone=13900009999 person=乙区物业 sig=data:image/png;base64,ZZZ pets=1
>>> gov_a        status=200  泄漏! phone=13900009999 person=乙区物业 sig=data:image/png;base64,ZZZ pets=1
>>> hospital_a   status=200  泄漏! phone=13900009999 person=乙区物业 sig=data:image/png;base64,ZZZ pets=1
>>> gov_city     status=200  泄漏! phone=13900009999 person=乙区物业 sig=data:image/png;base64,ZZZ pets=1
```

`gov_city` 本来就该看到全部；前三个都是越权。**同一个模块里
`capture_update` / `capture_delete` 早就走了 `_get_capture_for_user()`，
只有 `capture_detail` 漏了。**

修法：`capture_detail` 改走同一个 `_get_capture_for_user()`。
但**不能简单收口成一律 404** —— 医院跨区县送医是允许的
（`transfer_create` 不限制目标医院区县），只按区县过滤会让跨区县转运后
医院点开捕捉详情直接 404。所以 `_get_capture_for_user()` 给医院加了
「有动物转到过本院」的放行分支：

```python
qs = get_district_filtered_queryset(Capture, user)
if user.role == 'hospital' and user.institution_id:
    own = Capture.objects.filter(pet__hospital_id=user.institution_id).values('pk')
    qs = Capture.objects.filter(Q(pk__in=qs.values('pk')) | Q(pk__in=own)).distinct()
return qs.filter(id=pk).first()
```

**这一步最容易写错的地方**：只加拦截、不测放行，就会把跨区县送医这条正常
路径一起掐掉 —— 而只测拦截的用例会全绿。所以回归用例里
「甲区医院读乙区、但那单有动物转到过甲区医院」必须是 **200**。

### 2.22.3 缺陷二：区级管理员能在其他区县建机构

`institution_create` 只在**没传** `district_id` 时才按操作员区县兜底；
**传了就直接落库**，不校验是否在管辖范围内。而 `institution_edit` 有这条校验：

```python
# institution_edit 里早就有
if _district_out_of_scope(request, data['district_id']):
    return json_fail('无权将机构调整到其他区县')
```

实测：

```
>>> 区级管理员(甲区) 向他区(乙区)建机构: status=200 机构创建成功
>>> 乙区机构数 1 -> 2  ✗✗✗ 越权写入成功
```

新建机构**不会**被 `resolve_district_scope` 兜住 —— 那是给业务记录用的，
而且它禁止市级归属，机构却**可以**挂市级（现场两个捕捉点就挂在市级）。

修法：把判据提成一个共用函数，两个接口都调它：

```python
def _district_out_of_scope(request, district_id):
    """目标区县是否超出操作员的管辖范围（市级管理员永远返回 False）。"""
    scope = get_district_scope(request)
    return scope is not None and str(district_id) != str(scope)
```

### 2.22.4 缺陷三：把医院改挂市级 → 操作员获得全量可见（提权链）

`institution_create` 明确拒绝「医院挂市级」：

```python
if inst_type == 'hospital' and district.is_city:
    return json_fail('医院必须挂具体区县，不能挂市级')
```

`institution_edit` **没有这一条**。而机构换区县时会连带搬迁其下操作员
（`cascade_operator_district()`）。于是：

```
>>> 市级管理员把医院改挂市级: status=200 机构更新成功，1 个操作员的所属区县已同步调整
>>> 改后：医院区县=全市  操作员区县=全市
>>> 操作员自洽校验=该角色的账号所属区县必须是具体区县，不能挂市级
>>> 操作员可见范围（None=全部区县）=可见捕捉单 0 条（全量）
```

那个账号的 `district.is_city` 变成 `True` → `get_district_scope()` 返回 `None`
→ **整个市所有区县的数据都看得到**，而且它自己就违反了系统自己的不变式
`validate_operator_district()`。

> 这条链**完全靠数据编辑**完成，没有一处「看起来像越权」的请求。
> 界面上点不出来（区县下拉过滤掉了市级），**只有直接打接口才会暴露** ——
> 与上一轮 `canCreateRole` 是同一种「不可见」。

修法（`institution_edit`）：先算出**改动之后**的类型与区县，再按最终状态校验。
只看单个字段会漏掉「同时改 `type` 和 `district_id`」这种组合：

```python
new_type = data['type'] if data.get('type') in (...) else inst.type
new_district = <解析后的区县，或 inst.district>
if new_type == 'hospital' and new_district.is_city:
    return json_fail('医院必须挂具体区县，不能挂市级')
```

顺带补上 `institution_edit` 缺失的**电话格式**校验（create 有、edit 没有，
前端拦着、接口没拦）。

### 2.22.5 缺陷四：编辑挂市级的捕捉点，不改区县也会被静默改判

前端机构表单的区县下拉写成 `filter(d => d.status === 'active' && !d.is_city)` ——
**排除了市级**。而挂市级的捕捉点确实存在。于是打开编辑弹窗时
`<select>` 里没有机构当前的区县，退化成**选中第一项**；而保存时
`district_id` 是**无条件**从下拉里读的：

```js
district_id: document.getElementById('inst-district').value,   // 永远会发
```

**什么都不改、直接点保存**，机构就被搬到了某个具体区县，还会连带搬走它下面的操作员。
实测（临时关掉前端兜底，模拟修复前）：

```
[FAIL] C1 下拉默认选中机构**当前**区县「全市（市级）」（实际「襄城区」）
[FAIL] C2 挂市级的机构仍能看到「全市（市级）」选项（否则保存必被改判）
[FAIL] C3 不改区县直接保存后，机构仍在「全市（市级）」（实际「襄城区」）
```

这是一条**纯静默**的数据损坏：界面上没有任何提示，机构列表里那一行的区县
就这么变了。修法两条：

1. 编辑时把机构**当前**区县兜底加回选项（哪怕它是市级）；
2. 区级管理员的区县下拉收敛到本区县（原先列全部 4 个，选了他区才被服务端拒绝，
   白填一遍 —— 与 2.21 给账号表单做的是同一件事，机构表单漏了）。

### 2.22.6 本轮新增测试（21 个，685 → 706）

| 用例组 | 数量 | 锁什么 |
|---|---|---|
| `CaptureDetailDistrictScopeTest` | 9 | 三端跨区县读 404 + **响应体不含 PII** + 5 条正向对照（本区县、市级、跨区县送医） |
| `InstitutionApiTest` 新增 | 8 | 他区建机构被拒且不落库、正向对照、不传 `district_id` 兜底；医院挂市级被拒 + **操作员不得被连带搬走** + 组合改动 + 正向对照（捕捉点挂市级仍允许）+ 电话格式 |
| `InstitutionFormScopeContractTest` | 4 | 源码级：create/edit **两侧都要有**「医院挂市级」与 `_district_out_of_scope`；前端下拉收敛 + 保留当前区县 |

两条值得单独说的：

- **`test_cross_district_read_does_not_leak_pii` 第一版是空转的。**
  它断言响应体里不含那个电话号码，但我忘了给那张单设 `contact_phone` ——
  断言在空字符串上永远成立。**变异验证时它没跟着变红**才暴露出来，
  已改成对三端各断言三个字段（电话 / 交接人 / 签名）。
  「拦截用例也要验证它真的会红」，这一条同样适用于安全用例。
- **`InstitutionFormScopeContractTest` 第一版四个用例全红**，两个原因都记下了：
  用 `brace_body` 取 Python 函数体会从签名后第一个 `{` 开始配对
  （那往往落在某个 dict 字面量里），Python 必须按顶层 `def` 行切；
  前端那边则要用 `m.start()` 而不是 `m.end()` 去定位方法体的 `{`。

### 2.22.7 变异验证

| 变异 | 变红的用例 |
|---|---|
| `capture_detail` 改回裸查 | 3 个（三端跨区县读） |
| `institution_create` 去掉区县范围校验 | 1 个 + 源码级契约 1 个 |
| `institution_edit` 去掉「医院挂市级」 | 2 个 + 源码级契约 1 个 |
| 前端去掉「保留机构当前区县」 | 源码级契约 1 个 + GUI `57` 的 C1/C2/C3 |
| 前端去掉「区县下拉收敛」 | 源码级契约 1 个 |

### 2.22.8 GUI 实测（`gui-test-scripts/57_institution_form_scope.js`，8/8 通过）

| 检查 | 结果 |
|---|---|
| A1/A2 区级管理员机构表单只列本区县（襄城区） | ✅ |
| A3 区级管理员仍能在本区县新建捕捉点（正向对照） | ✅ |
| B1/B2 绕过界面在他区建机构被服务端拦下、且不落库 | ✅ |
| C1/C2 编辑挂市级捕捉点时下拉默认选中「全市（市级）」 | ✅ |
| C3 **不改区县直接保存**后机构仍在「全市（市级）」 | ✅ |

脚本自带开场清理 + 收尾清理：机构**没有删除接口**，所以探针机构从数据库清，
清理前先做引用审计（有任何反向引用就跳过并提示），跑完演示数据完全还原
（已核对：捕捉点区县、操作员区县、无残留机构）。

### 2.22.9 本轮验证结果

| 项 | 结果 |
|---|---|
| 全量测试 | **706 通过**（685 → +21） |
| `46` 全端冒烟 | 80 个视图 0 报错 0 空白 |
| `57` 机构表单区县规则 GUI 实测 | **8/8** |
| 变异验证 | 5 组全部被抓到 |
| 演示数据 | 跑完已还原（机构区县 / 操作员区县 / 探针机构均已核对） |

### 2.22.10 已知边界

- `capture_detail` 给医院的「有动物转到过本院」分支**目前 UI 没有用到**
  （医院端不调 `getCapture`）。保留它是为了不破坏既有契约
  （`test_hospital_can_view_capture_detail`），并让跨区县送医后的详情可用。
- ~~仍**没有系统扫过**「`disabled` / `readonly` / 选项里没有」这三种前端形态~~
  **第三种已在第十七轮做完**（见 2.23，命中 3 处越权/越界）；
  `disabled` / `readonly` 两类仍是抽查：本轮抽查的几处
  （捕捉单编辑/删除按钮的 `disabled`、库存 ≤ 0 物料的 `disabled`、
  批号 `readonly`、回收单号 `readonly`）后端都有对应校验或本来就不接受客户端值，
  但不是穷举。
- `user_edit` 仍然不存在（见 2.21.8），补的时候必须复用 `validate_user_manage_scope()`。

---

## 二点二十三、前端收窄选项 · 后端不校验（2026-09-18 第十七轮）

### 2.23.1 方法：把「下拉里根本没有这个选项」当成一个检查项

上一轮（2.22）结尾留了一句「`disabled` / `readonly` / 选项里没有这三种前端形态
只抽查过、没穷举」。本轮把第三种做完 —— 它也是最危险的一种：

> **前端按某个规则把候选值过滤掉了，服务端对同一个字段不做任何校验。**

界面上根本点不出来，只有直接打接口才暴露。反过来还有一种对称的坑：
**界面上能改、服务端却要拒** —— 用户改一下就撞 400，看不出原因
（本轮 A、C 两处都是这样，所以修复是「后端补校验 + 前端跟着禁用」成对做的）。

做法：把四端所有 `<select>` 列出来，逐个问两个问题 ——
① 选项是被什么规则收窄的？② 服务端对这个字段有没有同一条规则？

本轮命中三处，且都带**跨模块孪生漂移**的性质：同一个判据在一个模块里写了、
在另一个模块里漏了。

### 2.23.2 一、区县级别（`is_city`）翻转 = 给整个区县的账号发全市读权限

`get_district_scope()` / `get_district_filtered_queryset()` 对「所属区县
`is_city=True`」的账号返回 **None = 全部数据**。而 `district_edit` 里
`is_city` 是可以随手改的一个字段：

```python
if 'is_city' in data:
    district.is_city = bool(data.get('is_city'))   # 零守卫
```

于是把**已有账号挂靠**的区县改成「市级」，等于一次性给该区县下所有账号
发放全市读权限 —— 不需要碰任何账号，也不会有任何一个请求看起来像越权。
反向（市级降级）会让市级账号失去全部可见性，同样静默。

实测（甲区账号读全市统计）：

| 步骤 | 甲区区级管理员的 `pet_total` |
|---|---|
| 翻转前 | 1（只看到本区） |
| 把甲区改成市级后 | **2（两个区的都看到了）** |

这条链还**跨模块**绕过了上一轮刚加的守卫：`institution_edit` 明确拒绝
「医院挂具体区县以外的市级」，而把一个区县整体翻成市级，会让它下面
**所有**医院都变成「挂市级」—— 上一轮堵住的那条路，这一轮从旁边开回来了。

**修法**：`is_city` 变更前先做引用审计，复用 `district_delete` 的那份清单
（抽成共用函数 `_district_references()`）。删除与改级别必须用同一份清单，
否则会出现「删不掉但能改级别」的缝隙。空区县（无账号/无数据）仍可自由设定级别 ——
正向对照用例锁住这一点，避免把守卫做成「一律禁止」。

### 2.23.3 二、停用区县仍可作为机构归属

前端机构表单的区县下拉写的是 `d.status === 'active' && !d.is_city`；
上一轮补了 `!is_city`、**漏了 `status`**。而同一个模块的 `user_create`
两条都校验了（「所选区县已停用」）—— 同一模块内两条创建路径各写一份判据。

实测：停用南漳县后，`institution_create` 仍能往它里面建机构（返回 200）。

**修法**：抽 `_inactive_district_error()`，`institution_create` /
`institution_edit` 共用，与 `user_create` 同一条判据。
编辑侧**只拦「搬到停用区县」**：原地改名必须放行，否则停在停用区县里的机构
连名字都改不了，只能靠改库。

### 2.23.4 三、捕捉点与归属区县可以不一致 → 捕捉单从执行机构的可见范围里消失

捕捉单的区县隔离按 `Capture.district` 过滤，而捕捉点（执行机构）物理上坐落在
某个区县。前端只在捕捉点下拉的 `change` 事件里「自动同步」归属区县，
**下拉本身没有禁用**，后端也不校验 —— 于是可以提交
「甲区捕捉点 + 乙区归属区县」。

后果不是「报错」，而是**静默错位**：这张单落到乙区，甲区捕捉点操作员
（真正执行的人）在区县隔离下**看不到自己登记的单**，既不能转运也不能作废；
而乙区政府看到一张「甲区捕捉点执行」的单。

实测（`hd_shelter` 挂在「全市（市级）」下，绕过区县范围校验直达一致性校验）：

```
提交「樊城流浪动物捕捉点（樊城区）+ 归属襄城区」
→ success=false，捕捉点「樊城流浪动物捕捉点」属于樊城区，归属区县必须与之一致
→ 捕捉单数 12 → 12（没有落库）
```

**修法**：抽 `_shelter_district_conflict()`，`capture_create` 与
`capture_update` 共用。**捕捉点挂在「全市（市级）」时不约束** ——
现场两个捕捉点就挂在市级，那种情况下归属区县必须由操作员指定
（`resolve_district_scope` 本就禁止市级归属）。编辑侧只在**确实换区县**时校验，
让历史错配的单仍能原地改其它字段、也能改回一致。

前端同步做了两处，否则就是「界面上能改、服务端要拒」的死胡同：

- 新建捕捉单：捕捉点挂在具体区县时，归属区县下拉**禁用**并显示
  「已按执行捕捉点所在区县锁定为「襄城区」」；捕捉点挂市级时才放开。
- 编辑捕捉单：同样锁，但**历史错配的单保持可改**（`districtLocked` 只在
  「本单区县 == 捕捉点区县」时为真），留一个改回来的入口。
- 区县表单补一句提示：已有账号/数据挂靠时级别不可修改。

### 2.23.5 新增 23 个测试（706 → 729）

- `DistrictIsCityGuardTest`（6）：翻转被拒且不落库、**翻转后区级账号的可见范围
  不得扩大**（`pet_total` 2→1 的主断言）、市级降级被拒、空区县仍可设市级、
  普通改名不受影响、提交同值算 no-op；
- `InactiveDistrictAssignmentTest`（5）：建机构被拒且不落库、机构搬入停用区被拒、
  正向对照两条、锚点用例（`user_create` 早已校验「所选区县已停用」）；
- `CaptureShelterDistrictConsistencyTest`（6）：创建/编辑两路拒绝 + 四条正向对照
  （同区县放行、不传区县仍按捕捉点兜底、市级捕捉点可指定任意区县、
  拦截时**宠物档案的区县也没被搬走**）；
- `NarrowedOptionContractTest`（6，源码级）：`district_edit` 的引用审计、
  删除与改级别共用清单、机构创建/编辑都校验停用区县、
  捕捉单创建/编辑共用一致性判据、前端两处下拉锁定。

### 2.23.6 变异验证（6 组全部被抓到）

| 组 | 拆掉的东西 | 结果 |
|---|---|---|
| M1 | `capture_create` 的一致性校验 | ✓ 被抓到（API 用例 + 契约用例各红一条） |
| M2 | `capture_update` 的一致性校验 | ✓ |
| M3 | `institution_create` 的停用区县校验 | ✓ |
| M4 | `district_edit` 的引用审计 | ✓ |
| M5 | 前端新建表单的下拉锁定 | ✓ |
| M6 | 前端编辑弹窗的下拉锁定 | ✓ |

**M5 第一版是空转的**：断言只写了 `districtSelect.disabled =`，
把右侧改成 `false`（写了但没生效）照样能过 —— 是变异验证时它没跟着变红才暴露的。
断言改成必须匹配 `districtSelect.disabled = locked`。
安全/守卫类用例尤其要跑变异，这类「断言在错误实现上仍然成立」的形态很隐蔽。

### 2.23.7 GUI 实测（`gui-test-scripts/58_narrowed_options.js`，20 项检查 0 失败）

| 段 | 检查内容 |
|---|---|
| A | 捕捉点账号的新建表单：归属区县被禁用、锁到襄城区、提示文案正确、选项未被裁掉 |
| B | 政府端选捕捉点：挂市级时**可改**，换成具体区县捕捉点后锁死到该区县 |
| C | 绕过界面提交不一致区县 → 服务端拒绝 + 捕捉单数不变（12 → 12） |
| D | 停用南漳县后建机构被拒 + 未落库，收尾恢复启用 |
| E | 有数据的区县改市级被拒（错误信息列出全部 13 类引用）+ 空区县仍可改（正向对照） |
| F | 编辑捕捉单弹窗：归属区县同样锁定并提示「由执行捕捉点所在区县确定，不可修改」 |

F 段是特意加的：`showCaptureEdit` 这类弹窗方法本轮被改过
（新增了一次取机构列表的异步调用与一个局部变量），而历史上这类方法出过
「引用了外层作用域的变量 → 弹窗整体打不开」的 `ReferenceError`，
所以改完必须实测一次「弹窗能不能正常打开、字段状态对不对」。

截图：`gui-test-screenshots/r17_narrowed_a_locked.png`（捕捉端新建表单锁定）、
`r17_narrowed_b_follows_shelter.png`（政府端跟随捕捉点）、
`r17_narrowed_c_edit_locked.png`（编辑弹窗锁定）。

**顺带修掉脚本 56 的数据卫生缺陷**：`56_operator_district.js` 会建一个
`r15_match_probe` 探针账号，而政府端用户管理**没有删除入口**，脚本原先只在注释里
让「人工执行」清理。结果这个账号连续两轮绊倒后续运行 ——
「前提：该医院下只有 huimin_hosp 一个操作员」不成立，
连锁红三条（提示里的操作员数量、一致时能否建号），
**看起来像本轮改动把功能改坏了，实际是脏数据**。
现在脚本用 `cleanProbeAccounts()` 在开场 + 收尾各清一次（中途抛异常时收尾不执行，
所以开场也要清）。本轮的 58 同样按前缀清理，并注意
`audit_logs` 不算阻塞引用（`AuditLog.district` 是 `SET_NULL` 且自带名称快照）。

### 2.23.8 本轮验证结果

| 项 | 结果 |
|---|---|
| 全量测试 | **729 通过**（706 → +23） |
| `46` 全端冒烟 | 80 个视图，0 JS 报错 0 空白 |
| `47` 筛选条守卫 | 20/20（3 跳过） |
| `56` 账号区县一致性 | 0 失败（并已改为自动清理探针账号） |
| `58` 收窄选项 | **20/20** |
| 变异验证 | 6 组全部被抓到 |
| 演示数据 | 跑完已完全还原（区县 / 机构 / 操作员 / 捕捉单 / 宠物数均已核对） |

### 2.23.9 已知边界

- 本轮只覆盖了**下拉/选项**这一类。其余两类（`disabled`、`readonly`）
  **已在第十八轮穷举完成**（见 2.24.1）：四端 7 处锁控件，6 处服务端对齐，
  1 处（定位地址/经纬度）服务端接受但界面无入口 —— 该字段在模板里只有渲染处、
  没有读取点，界面从不提交，无害。
- `capture_create` 的捕捉点**启用状态**未校验（前端那个下拉也没过滤停用机构），
  **已在第十八轮前后端同时收口**（见 2.24）。顺带发现同一原则下的第三处：
  停用区县仍可作为捕捉单归属。
- `district_create` 允许直接建一个 `is_city=True` 的区县（新库、无引用），
  这是「新建市级区县」的正常路径，未加限制。
- 脚本 58 会临时停用/启用区县，中途抛异常会留下停用状态；
  开场清理只处理 `R17*` 前缀的机构与区县，不会自动恢复停用状态。

---

## 二点二十四、「停用」状态必须真的生效（2026-09-18 第十八轮）

上一轮结尾留了一句「`disabled` / `readonly` / 选项里没有这三种前端形态只抽查过」。
本轮先把剩下的两种扫完，顺着它撞到一个更基本的问题：**`Institution.status`
在整个后端没有任何读取点** —— 「停用」只是个徽标。

### 2.24.1 先把 `disabled` / `readonly` 扫完：这一类基本干净

做法：把四端模板里所有 `disabled` / `readonly` 字面量，以及**动态设置**的形态
（`.disabled =` / `.readOnly =` / `setAttribute('disabled')` / `pointer-events:none`
/ `<fieldset disabled>`）全部列出来，逐个问「服务端对这个字段做不做同一套校验」。

四端共 7 处真正锁住值的控件，6 处是对齐的：

| 锁住的控件 | 服务端的对应约束 |
|---|---|
| 捕捉单列表的**编辑/删除**按钮（`locked ? tip + dis : ''`） | `capture_transfer_state()` 返回 `can_edit` / `can_delete` |
| 编辑弹窗的**归属区县**下拉（`districtLocked`） | 上一轮加的 `_shelter_district_conflict()` |
| 疫苗/驱虫下拉里**库存 ≤ 0 的 `<option disabled>`** | `get_hospital_stock(...) < qty` 直接拒 |
| 回收单号 `#rc_ledgerNo`（`readonly`） | 服务端 `generate_ledger_no('RET')` |
| 编辑弹窗的**捕捉点**（`#ed_shelterName` `disabled`） | `shelter` 不在 `updatable` 元组里，服务端直接忽略 |
| 医院端宠物档案的芯片号（`readonly`） | 纯展示，无写入路径 |
| 定位按钮 / 提交按钮的 `btn.disabled`（加载态） | 与业务校验无关 |

唯一「服务端接受而界面无入口」的是捕捉单的**定位地址 / 经纬度**：
`capture_update` 的 `updatable` 里有 `geo_address`，另有
`for field in ('latitude', 'longitude')` 一段，而编辑弹窗里那两个框是 `disabled`
的。但它们在整个模板里**只有渲染处、没有任何读取点** —— 界面从不提交这两个值，
所以既改不了也无害，本轮不动它。

> **这一类里没有发现越权/越界**。记录在此，免得下一轮再从头扫一遍。

### 2.24.2 顺着它查出来的真问题：`Institution.status` 零效力

`Institution.status` 在整个后端只有**两处写入** —— `institution_create` 写
`status='active'`、`institution_toggle_status` 翻转 —— **没有任何一处读取**。
后果：

- 停用一家医院后，新建转运单的医院下拉照旧列出它、`transfer_create` 照旧接受，
  动物被「送」到一家已经不运营的医院；
- 停用一个捕捉点后，照旧能对它登记新的捕捉单；
- `institution_toggle_status` **不联动其下账号**（操作员照旧能登录、照旧能签收）。

前端只有一半写了规则：**政府端**三处机构下拉过滤了 `status === 'active'`
（机构表单、捕捉点选择、转运单医院选择），**捕捉点端**三处没有
（新建捕捉单的捕捉点、转运单的接收医院、下发出库的接收医院）。
同一条规则只写了一半 —— 典型的跨端孪生漂移，而服务端两侧都没有，
所以「停用」在整条链路上等于没写。

### 2.24.3 同族第三处：停用区县仍可作为捕捉单的归属区县

`resolve_district_scope()` 只校验 `is_city`、不看 `status`；捕捉点端那个归属区县
下拉只过滤了 `!isCity`。而 `_inactive_district_error()` 的注释声称
「**前端所有区县下拉都写了 `d.status === 'active'`**」—— 实测不成立。
注释写的是意图，不是事实；这类「注释与代码不符」比没有注释更危险。

### 2.24.4 修法

**判据提成共用函数**（`business/services.py`）：

- `inactive_institution_error(institution, label)` —— 新增
- `inactive_district_error(district)` —— 新增，`supervision.views._inactive_district_error()`
  与 `user_create` 都改成它的**薄封装**，实现只留一份。
  同一个概念原先散在三处，正是漂移的温床。

**拦截点**：

| 接口 | 拦什么 |
|---|---|
| `capture_create` | 停用捕捉点；**显式提交**的停用归属区县 |
| `transfer_create` | 停用**来源捕捉点**；停用**接收医院** |
| `capture_update` | 只在**确实换区县**的分支里拦停用区县 |

`transfer_create` 的医院校验有两个硬要求：**前置到写库之前**（本函数是边校验边
落库 —— 循环里 `Transfer.objects.create` + 改宠物 `hospital`，在循环里
`return json_fail` 会留下半截转运单和已被改走归属的宠物），且**覆盖所有 item**
（拆分下发时只看第一条会让第二段的停用医院漏过去）。

**只拦新引用，不拦存量**：停用医院上已下发的转运单仍要能签收，停在停用捕捉点 /
停用区县下的历史捕捉单仍要能改名、能转运。否则一次停用就把历史业务全锁死，
只能靠改库。

**前端配套**（否则是「界面上能选、服务端要拒」的死胡同）：

- 捕捉点端三处机构下拉补 `status === 'active'`，与政府端对齐；
- 两个区县下拉补 `status === 'active'`，但**必须保留「当前值」的兜底**
  （`|| d.id === full.districtId`）—— 只过滤会让存量记录的下拉退到第一个选项，
  归属区县被**静默改掉**，或者撞 400 而用户无法修正。

### 2.24.5 新增 17 个测试（729 → 746）

| 用例类 | 数 | 锁定的内容 |
|---|---|---|
| `InactiveShelterGuardTest` | 6 | 建单被拒且**宠物档案也没生成**；不传 `shelter_id` 走账号绑定机构同样被拦；停用捕捉点发转运被拒；存量捕捉单仍能改名 |
| `InactiveHospitalGuardTest` | 6 | 收转运被拒且**宠物 `hospital` 没被改走**；拆分下发有一家停用医院则**整单**拒绝（不留半截）；停用前已下发的转运单**仍能签收** |
| `InactiveDistrictOnCaptureTest` | 5 | 创建/编辑两路拒绝 + 两条正向对照（原地改名不受影响） |
| `InactiveEntityContractTest` | 4 | 源码级：创建路径共用判据、判据单一实现、捕捉点端下拉过滤、区县下拉保留当前值 |

「拦截时连带写入也没发生」是本轮的主断言：转运单被拒但宠物的 `hospital` 被改走，
会让动物卡在一家没人运营的医院上，既回不到可转运池、也没人能签收。

### 2.24.6 变异验证（8 组全部被抓到）

| 变异 | 变红用例数 |
|---|---|
| M1 `capture_create` 不校验捕捉点状态 | 2 |
| M2 `transfer_create` 不校验来源捕捉点 | 1 |
| M3 判据恒返回 `None`（所有守卫失效） | 5 |
| M4 医院校验只看第一条 item | 1 |
| M5 `capture_create` 不校验归属区县状态 | 1 |
| M6 捕捉点端机构下拉不过滤停用 | 1 |
| M7 区县下拉去掉「当前值」兜底 | 1 |
| M8 `supervision` 不再复用服务层判据 | 1 |

**M8 第一版是空转的**：断言只写了 `assertIn('inactive_district_error(', body)`，
结果被**文档字符串里那句 `services.inactive_district_error()`** 命中 ——
把实现改回各写一份，断言照样通过。**是变异验证时它没跟着变红才暴露的。**

改成断言真正的委托语句 `^\s+return inactive_district_error\(district\)`
（且必须带 `re.M` —— 第一版漏了，`^` 只匹配字符串开头，在未变异的代码上就红了），
再加一条反向断言 `district.status != 'active'` 不得出现。

> 这是本项目**第二次**由变异验证抓出空转（上一次是上一轮的
> `districtSelect.disabled =` 没带右侧取值）。规律很清楚：
> **断言越像「检查某个写法出现过」越容易空转。**

### 2.24.7 GUI 实测（`gui-test-scripts/59_inactive_entity.js`，9 项检查 0 失败）

用真实演示数据，临时建三个探针（捕捉点 / 医院 / 区县）并全部停用：

```
A 捕捉点下拉（7 项）：请选择捕捉点 / 襄城… / 樊城… / 东津新区… / 襄州区… / 南漳县… / R18市级探针捕捉点
B 归属区县下拉（5 项）：襄城区 / 樊城区 / 东津新区 / 襄州区 / 南漳县
PASS A1 停用的捕捉点不出现在新建捕捉单的捕捉点下拉里
PASS A2 正向对照：启用的捕捉点仍然列得出来（不是把下拉清空了）
PASS B1 停用的区县不出现在归属区县下拉里
PASS B2 正向对照：启用的具体区县仍然列得出来
C1 停用捕捉点建捕捉单：false / 捕捉点「R18探针捕捉点」已停用，不能用于新的业务记录
C2 停用捕捉点发转运单：false / 捕捉点「R18探针捕捉点」已停用，不能用于新的业务记录
C3 停用医院收转运单：false / 接收医院「R18探针医院」已停用，不能用于新的业务记录
C4 停用区县作归属：false / 所选区县已停用
PASS C1b 且没有落库（捕捉单 12 → 12）
PASS C2b/C3b 且没有落库（转运单 15 → 15）
```

C4 特意用一个挂在「全市（市级）」的探针捕捉点：那样归属区县完全由操作员指定，
撞到的才是「区县已停用」这条判据，而不是先撞上「捕捉点 ↔ 归属区县不一致」。

### 2.24.8 本轮验证结果

| 项 | 结果 |
|---|---|
| 全量测试 | **746 通过**（729 → +17） |
| `46` 全端冒烟 | 80 个视图 0 报错 0 空白 |
| `47` 筛选条守卫 | 20/20（3 跳过） |
| `58` 收窄选项（上一轮） | 0 失败 |
| `59` 停用状态（新增） | **9/9** |
| 变异验证 | **8 组全部被抓到** |
| 数据一致性巡检 | 0 问题（退出码 0） |
| 演示数据 | 跑完已完全还原（12 捕捉单 / 32 宠物 / 15 转运单 / 无探针残留） |

### 2.24.9 已知边界

- **`institution_toggle_status` 刻意不联动其下账号**：联动会让停用医院的在途
  转运单没人签收。本轮只拦新引用，存量由原操作员继续收尾。这是一个策略选择，
  不是遗漏。
- `capture_update` 不接受 `shelter_id`（捕捉点不可改），所以编辑侧没有
  「换捕捉点」这条需要校验状态的路径。
- 停用机构仍会出现在**筛选条**的选项里（如按医院筛选历史转运单）——
  这是刻意的：存量记录需要按它筛选。
- `disabled` / `readonly` 这一类本轮已穷举，但**只覆盖四端 `portal.html`**；
  根目录的早期静态原型（`shelter.html` 等）不在范围内。

---

### 2.25 第十九轮：接口 × 角色矩阵 —— 编号规则配置的读/写权限必须分开

**方法**：把扫描形态从「某个字段 / 控件」换成**「接口 × 角色」矩阵**。
从 Django 的 URL 解析器反解出每条 API 的 `role_required` 角色集合，再把四个门户
实际调用的接口（`tnr-api.js` 的具名封装 + 模板里的直连与拼接调用）拉平，
找**死按钮**（界面有入口但本角色无权，点一下必报错）。

**矩阵本身是干净的**：

- 前端 **0 处悬空调用**：67 条 URL 字面量全部命中后端路由；
- 后端 78 条 API 初筛出 11 条「前端零调用」，逐条核实**全部是扫描盲点** ——
  前端用的是 `TNR_API.BASE + 'xxx/' + id + '/yyy/'` 拼接形态，正则漏了。
  真冗余只有 `/api/me/`（仅测试使用，前端靠服务端注入的 `window.TNR_USER`）；
- 逐端口核对：`shelter` 41 个封装、`hospital` 24 个、`adopter` 7 个、`gov` 25 个，
  **全部在本角色权限内**。

**唯一的真问题**：`/api/supervision/config/` 把 **GET 也限死成 `gov_city`**。

政府端「系统配置」页对区级管理员是**可见**的 —— 同一页的「操作日志」标签页本就
允许区级访问（`getOperationLogs` 允许两级），前端也按「可读」渲染编号前缀表，
提示文案写的正是「仅市级管理员可**修改**编号规则配置」。于是区级打开这一页时：

```
GET /api/supervision/config/ → 403
  → TNR_API._get() 静默返回 []
  → config[key] || '' → 空串
  → 9 个真实的 CAP / TRF / TRE / … 前缀被渲染成**空白输入框**
     （无任何报错，输入框可编辑，还带一个可点的「保存配置」）
```

用户看到的是「编号规则没配置」。这与项目明令禁止的**「兜底谎报业务状态」**
（`status || 'completed'`）同族：**无权限时不能拿空值冒充真实值**。

而且它还被一条测试锁着：`test_system_config_gov_district_403` 同时断言了 GET 与
POST 都 403。后端「有意」、前端「按可读渲染」—— 典型的前后端契约不一致，
只是这次没人注意到前端那一半。

#### 2.25.1 修法：读放开、写收紧

- **后端**：装饰器改 `@role_required('gov_city', 'gov_district')`，
  POST 分支内加市级守卫 `if request.user.role != 'gov_city': return json_fail(…, status=403)`；
- **前端**：`renderSettingsContent` 用 **`TNR_API.get(…)`（失败会抛错）** 取代静默封装，
  读失败显式渲染「加载配置失败」；非市级输入框加 `readonly`、隐藏「保存配置」按钮
  —— 提示文案「仅市级管理员可修改」这才成立；
- **顺手清坑**：删掉已成死封装的 `TNR_API.getSystemConfig()`，并在 `_get()` 上写明
  危害 —— **列表页可以用它（空表格 ≈ 没有数据），凡「值必须如实显示」的场景
  一律不能用**。

**为什么不是「把整页对区级隐藏」**：该页的「操作日志」标签页本就对区级开放，
说明这个页面是给区级用的；把整个导航项藏掉反而让区级失去操作日志。

#### 2.25.2 新增 6 个测试、删除 1 个（746 → 751）

被删掉的是 `test_system_config_gov_district_403` —— 它把「GET 也 403」锁死，
正是本轮要纠正的契约。

- `test_system_config_gov_district_can_read`：区级读到**真实**前缀（`CAP` / `TRF`），不是空串；
- `test_system_config_gov_district_cannot_write`：POST 403 **且库里的值未被改动**；
- `test_system_config_other_roles_403`：捕捉点 / 医院 / 领养人读写一律 403；
- `SystemConfigPageContractTest`（源码级 2 条）：装饰器必须含两级 + POST 保留市级守卫
  + 反向断言整函数不得退回只限市级；前端必须用 `TNR_API.get(…)`、有 `readonly` 门禁、
  保存按钮受同一变量门禁、失败要显式渲染；
- `test_silent_config_wrapper_stays_removed`：`getSystemConfig` 不得被加回来，
  且 `_get()` 的危害必须在源码里写明。

#### 2.25.3 变异验证（6 组全部被抓到）

| 变异 | 结果 |
|---|---|
| M1 后端装饰器退回只限市级 | ✓ 被抓到 |
| M2 删掉 POST 的市级守卫 | ✓ 被抓到 |
| M3 前端输入框去掉只读门禁 | ✓ 被抓到 |
| M4 前端退回静默封装读配置 | ✓ 被抓到 |
| M5 前端保存按钮不再受门禁 | ✓ 被抓到 |
| M6 前端读失败不再显式报错 | ✓ 被抓到 |

**M1 第一版「空转」，但原因是变异脚本本身**：`src.replace(old, new, 1)` 命中了文件里
**更早出现的同名装饰器**（别的视图），根本没改到 `system_config`。测试全绿不是断言弱，
而是**变异没生效**。教训：**变异验证必须先确认「变异真的落到了目标代码上」**，
否则它证明的是脚本没跑对，而不是测试有效。

GUI 层也做了一次变异：后端退回只限市级并重启服务后重跑 `60`，**7 项打红**
（区级页面变成显式「加载失败」空态）—— 证明该脚本不是空转。

#### 2.25.4 GUI 实测（`gui-test-scripts/60_settings_permission.js`，16 项 0 失败）

- **A. 区级（`cy_gov`）**：9 个前缀全部渲染、值是**真实值**（`CAP` / `TRF`）、
  输入框**全部只读**、**无**「保存配置」按钮、显示「仅市级管理员可修改」提示；
- **B. 市级（`admin`）**：同样真实值、输入框**可编辑**、**有**保存按钮、无区级提示；
- **C. 反向对照**：区级绕过界面直接 POST → **403**，拒绝原因写明「仅市级管理员」，
  且**库里的值未被改动**。

#### 2.25.5 本轮验证结果

| 项 | 结果 |
|---|---|
| 全量测试 | **751 通过**（746 −1 +6） |
| `46` 全端冒烟 | 80 个视图 0 JS 报错 0 空白页 |
| `47` 筛选条守卫 | 20/20（3 跳过） |
| `58` 收窄选项 | 0 失败 |
| `59` 停用状态 | 0 失败 |
| `60` 编号规则权限（新增） | **16/16** |
| 变异验证 | **6/6 被抓到** + GUI 层变异 7 项打红 |
| 演示数据 | 本轮为只读验证，未改动任何业务数据 |

#### 2.25.6 已知边界

- **`_get()` 的静默语义没有改**：它被约 50 处调用，列表页依赖「失败 → 空数组」。
  本轮只删掉了在「值必须如实显示」场景误用它的那一个封装，并在源码里写明危害。
  更彻底的做法是让 `_get()` 在 401 / 403 / 5xx 时也抛错，但那会牵动全部列表页，
  需要单独一轮。
- **角色矩阵扫描只覆盖四端 `portal.html`**（含 `tnr-api.js` 封装）；
  根目录早期静态原型不在范围内。
- `/api/me/` 前端零调用（前端用服务端注入的 `window.TNR_USER`），保留给测试与
  程序化调用，不是缺陷。
- `adoption_hall_list` / `adoption_hall_detail` 是**有意公开**的领养大厅接口
  （无 `role_required`，靠 `is_active` + `pet__is_deleted=False` 过滤），
  故未纳入角色矩阵核对。

---

### 2.26 第二十轮：同一角色、同一区县内的横向越权（机构级隔离）

第十九轮扫的是**角色维度**（这个角色能不能调这个接口）。这一轮往下一层：
**同一角色、同一区县内的横向越权** —— 机构级隔离。

侦察从「现有查询到底按什么维度收敛」开始，机械列出各视图里所有
`institution=` / `institution__` / `hospital=` / `shelter=` 的写法：
**写入侧基本都有机构校验，读取侧几乎看不到按机构过滤** —— 这就是本轮的靶子。

#### 2.26.1 测试场：襄城区有两家医院

| 机构 | 区县 | 账号 |
|---|---|---|
| 爱心宠物医院（id=3） | 襄城区 | `aixin_hosp` |
| 瑞鹏宠物医院（id=4） | 襄城区 | `ruipeng_hosp` |

**同一区县存在两个同类型机构**，是横向越权的必要条件。用 `aixin_hosp` 身份拉各列表：

| 接口 | aixin 条数 | ruipeng 条数 | 结论 |
|---|---|---|---|
| `/pets/` | 5 | 2 | 各自本院 ✅ |
| `/pets/archive/` | **16** | **16** | 襄城区总共只有 7 只宠物 ❌ |
| `/transfers/` | — | — | 按 `to_hospital` 收敛 ✅ |
| `/treatments/` | — | — | 按 `hospital` 收敛 ✅ |
| `/adoptions/` | **含他院** | **含他院** | 按区县收敛 ❌ |

`/pets/archive/` 返回的 16 条里，有**从未进过本院**的动物，还带主人电话
（`intake_contact`）。而 `/pets/` 只返回本院 5 条 —— **同一角色、同类概念的两个接口
口径相反**。

#### 2.26.2 根因：不是「口径宽松」，是同类接口之间漂移

代码级判定（看每个列表函数有没有角色分支、有没有机构过滤）后，真相清楚了：

| 接口 | 医院角色的收敛维度 | 对不对 |
|---|---|---|
| `/pets/`（`hospital_pets`） | `hospital_id=user.institution_id`（注释：「医院只看分配给自己的宠物」） | ✅ |
| `/hall-listings/` | `hospital_id=user.institution_id` | ✅ |
| `/treatments/`、`/transfers/`、`/materials/transactions/`、`/euthanasia/` | 本机构 | ✅ |
| **`/pets/archive/`** | `get_district_filtered_queryset` → **本区县** | ❌ |
| **`/pets/<id>/lifecycle/`** | `Q(hospital_id=…) \| Q(district_id=…)` → **本院 ∪ 本区县** | ❌ |
| **`/adoptions/`** | `get_district_filtered_queryset` → **本区县** | ❌ |

所以医院**本该**是本机构口径，只有这三个漏了。**「医院就该看全区」这个解释站不住
—— 同一个「动物」概念的其它 6 个接口全是机构级。**

`Adoption` 直接挂在宠物上，领养记录含领养人姓名 / 电话；`/pets/archive/` 含
`intake_contact`（主人联系方式）。跨机构读这些字段是**横向越权**，不是「口径宽松」。

**影响面要分开看**（本轮逐一核对了四端实际调用）：

| 接口 | 医院端有界面入口吗 | 越权可达性 |
|---|---|---|
| `/adoptions/` | ✅ 「领养信息维护」页（`TNR_API.getAdoptions()`） | **界面上就能看到**他院领养记录 |
| `/pets/archive/` | ❌ 只有捕捉点端与政府端用（`getPetArchive()` 仅 `shelter/portal.html` 命中） | 接口可达，界面不可达 |
| `/pets/<id>/lifecycle/` | ❌ 医院端无入口（仅捕捉点端与领养人端用） | 接口可达，界面不可达 |

后两个「界面不可达」**仍然是真实漏洞**：任何医院账号直接用接口就能拉到全区动物的
完整档案与主人电话，不需要猜主键、也不需要任何前端配合。
「界面上点不到」是**可用性**问题，不是**授权**问题 —— 授权必须在服务端收口。

#### 2.26.3 修法：抽一个共用判据，两处详情/列表都接上

新增 `business.services.hospital_pet_scope(user)`，返回 `Q`：

```python
if not user.institution_id:
    return Q(pk__in=[])                      # 无机构 → 空集，不能退化成 Q(hospital_id=None)
return (Q(hospital_id=user.institution_id)
        | Q(treatments__hospital_id=user.institution_id))
```

三个关键点：

1. **不能只按 `hospital_id`**：动物出院（放养 / 领养 / 主人领回 / 安乐死）后
   `Pet.hospital` 会被清空，只按它会让医院**查不到自己经手过的历史动物**。
   所以并上「本院诊疗过」（`treatments` 反向外键）。
2. **无机构必须空集**：写成 `Q(hospital_id=None)` 会匹配上所有「尚未分配医院」的动物
   —— 比按区县还宽。
3. **必须配 `.distinct()`**：`treatments` 是反向外键，JOIN 会按诊疗次数产生重复行。
   漏掉它不会少数据，只会让「一宠一档」的台账里出现重复行 —— 比少一行更难发现。

接入三处：`pet_archive`（档案列表）、`pet_lifecycle`（详情，越权返回 404）、
`adoption_list`（医院分支）。

#### 2.26.4 刻意**不改**的一处：捕捉点不许被改窄

改的过程中我一度把 `adoption_list` 的**捕捉点**分支也改成 `pet__shelter_id`
（理由是「与孪生接口 `adoption_application_list` 一致」）—— 全量测试立刻报红，
`test_list_keyword_and_status` 失败（那只宠物 `shelter=None`）。

查清后回退了，理由是这一支**不是漂移**：

- `/pets/`（`hospital_pets`）明确写着「捕捉点只看**本机构所在区县**的宠物」，
  并附了详细理由（现场把捕捉点操作员都挂在「全市（市级）」下，按 `user.district`
  会返回全市动物）；
- `adoption_list` 原来按 `Adoption.district` 收敛，**与它一致**；
- 真正与它们不一致的是孪生接口 `adoption_application_list`（按 `pet__shelter_id`，**更窄**）。

把 `adoption_list` 改成跟那个窄口径对齐，等于把「区县级收容入口」缩成「机构级」，
捕捉点会看不到**本区县、但动物并非由它捕捉**的领养记录。
**窄不等于对** —— 「口径不一致」有两种修法，往哪边对齐要看业务语义，不能默认往严的改。

> 残留漂移（本轮**未修**，记录在案）：`adoption_application_list` 的捕捉点分支
> 用 `pet__shelter_id`，比 `/pets/` 与 `adoption_list` 的区县口径**更窄**。
> 这是「少看到」不是「多看到」，不构成越权，故未在本轮扩大改动面。

#### 2.26.5 新增 18 个测试（751 → 769）

新建 `business/tests/test_hospital_scope.py`。**横向越权的判据是「两个同区县机构
的可见集合必须不相交」**，所以夹具在同一个区县里放两家医院，各带：

- 一只在治（`hospital=本院`）
- 一只已出院（`hospital=None`）但**由本院诊疗过**
- 一只本区县、但谁都没经手过（`plain`）
- 一只**不挂靠任何捕捉点**（`a_other`）—— 判别「捕捉点按区县 / 按机构」的关键夹具
- 一条**受理医院已被删除**的领养记录（`hospital=None`，`SET_NULL` 的真实形态）
- 一只别的区县的动物（验证区县隔离没被削弱）

| 测试类 | 覆盖 |
|---|---|
| `HospitalArchiveScopeTest` | 本院在治 ∪ 本院经手过；两院集合不相交；无机构空集；**无重复行**；捕捉点仍看全区；政府端不变 |
| `HospitalLifecycleScopeTest` | 他院在治 / 他院经手过 / 无关动物 / 跨区县 一律 404；本院在治 / 本院经手过 200 |
| `HospitalAdoptionScopeTest` | 医院只看本院；**与孪生接口医院分支一致**；无机构空集；**捕捉点保持区县口径（不许改窄）**；政府端不变 |
| `HospitalScopeContractTest` | 判据只留一份实现；`views_portal.py` 里 `hospital_pet_scope(` 恰好 3 次（定义 + 档案 + 溯源）；医院分支不得再出现 `Q(district_id=…)`；`adoption_list` 必须有医院分支 |

`test_shelter_scope_unchanged_district_wide` 里那句
`assertIn(self.adopt_other.id, ids, '此处失败说明捕捉点被误缩成机构级')`
就是 2.26.4 那个决定的**回归守卫**。

#### 2.26.6 变异验证（8 组全部被抓到）

| 变异 | 结果 |
|---|---|
| M1 判据去掉「本院诊疗过」这一半 | ✓ 被抓到 |
| M2 无机构时退化成按 `None` 过滤 | ✓ 被抓到 |
| M3 档案的医院分支失效（退回按区县） | ✓ 被抓到 |
| M4 档案漏掉 `.distinct()` | ✓ 被抓到 |
| M5 溯源退回「本院 ∪ 本区县」 | ✓ 被抓到 |
| M6 领养记录删掉医院分支（退回按区县） | ✓ 被抓到 |
| M7 领养记录无机构时退化成按 `None` 过滤 | ✓ 被抓到 |
| **M8 把捕捉点误缩成机构级（窄化）** | ✓ 被抓到 |

**M4 第一次空转，原因不是脚本 —— 是夹具造不出条件**（这是第十九轮教训的镜像）：
`test_no_duplicate_rows` 看着在查重复行，但夹具里每只动物**只有 1 条诊疗记录**，
反向 JOIN 根本产生不出重复行，漏掉 `.distinct()` 也照样绿。
补了**同院同动物的第二条诊疗记录**后 M4 立刻打红。

> 上一轮的空转是「变异没落到目标代码上」，这一轮的空转是「断言对、夹具造不出条件」。
> 两次都不是靠读代码发现的 —— **变异验证要连夹具一起怀疑**。

#### 2.26.7 GUI 实测（`gui-test-scripts/61_hospital_scope.js`，19 项 0 失败）

在**真实库 + 真实 HTTP 会话**上验证（只读，不改演示数据）：

| 组 | 检查 | 结果 |
|---|---|---|
| A | 医院端「领养信息维护」页真实渲染完成（该页调的就是 `/adoptions/`） | ✅ 两账号 |
| B | `/adoptions/` 返回集合 == ORM 里 `hospital_id=本院` 的集合；不含他院记录 | ✅ 两账号 |
| C | `/pets/archive/` == 「本院在治 ∪ 本院经手过」；不含他院专属动物 | ✅ 两账号 |
| D | 拉他院动物（id=67，属瑞鹏）的溯源被拒；拉本院动物正常 | ✅ |
| X | 同区县两家医院的档案集合**不相交** | ✅ 交集为空 |
| E | 捕捉点端档案**仍按区县**（口径未被改窄） | ✅ |

真实库实测数字：

| 账号 | 修复前 | 修复后 | ORM 真值 |
|---|---|---|---|
| `aixin_hosp`（爱心） | 16 | **5** | `[64,65,68,69,70]` ✅ 完全一致 |
| `ruipeng_hosp`（瑞鹏） | 16 | **4** | `[66,67,71,72]` ✅ 完全一致 |

**脚本自带「判别力自检」**：把「旧代码（按区县）会返回什么」也算出来，新旧同值时
显式打 `INFO` 而不是静默通过 —— 本轮它报出：

- `/pets/archive/` **有判别力**（16 vs 5 / 4）；
- `/adoptions/` **在当前演示库上无判别力**（区县 2 内一条领养记录都没有，新旧都返回 `[]`）
  → 该分支靠单测夹具覆盖，不靠 GUI 脚本。

这是第二十轮第二次撞到同一类问题（第一次是变异 M4 的夹具）。**「断言通过」和
「断言能失败」是两回事**，凡是靠数据比对的断言都要先问一句：这批数据能造出差异吗？

**踩到的环境坑（老坑重犯）**：改完 `views_adoption.py` 后直接用 HTTP 探测，
接口仍返回 16 条 —— 因为当时跑着的 `runserver` 是 **`--noreload`** 起的，
**代码改动不会生效**。重启（用普通 `runserver`）后立刻变成 5 条。
第六轮为模板踩过一次，这次为 Python 代码踩了一次：**改完必须先确认服务是新代码**，
否则整个 GUI 验证都是在验证旧版本。

#### 2.26.8 本轮验证结果

| 项 | 结果 |
|---|---|
| 全量测试 | **769 通过**（751 → +18） |
| 变异验证 | **8/8 被抓到** |
| GUI 实测（`61`，新增） | **19/19** |
| GUI 回归 | `46` 80 视图 0 JS 报错 0 空白；`47` 20/20（3 跳过）；`58` 0 失败；`59` 0 失败；`60` 16/16 |
| 修复前 / 后实测 | `aixin` 档案 16 → **5**（全本院）；`ruipeng` 16 → **4**（本院在治 2 + 本院诊疗过已出院 2） |
| 溯源接口 | 他院动物 **404**，本院 200 |
| 捕捉点 / 政府端口径 | **未变**（有测试锁死） |
| 数据一致性巡检 | 0 问题；本轮为只读验证，演示数据未改动 |

#### 2.26.9 顺带发现（**本轮未修**，见遗留建议）：4 个捕捉点账号被放大成全市可见

> **口径更正（2026-09-19 复核）**：「捕捉点账号挂在全市（市级）下」这个**事实本身
> 早有记录**（`MEMORY-deploy.md`：「`cy_shelter`/`hd_shelter` 的 `district_id` 是 1
> （全市/市级）…因此该账号能看到**全部**区县数据；验证区县隔离请用 `cy_gov`/`hd_gov`」）。
> 本轮**新的是**：① 把它与 `hospital_pets` 的处理**并排对照**，看出同一份代码对同一事实
> 处理相反；② **量化了影响面**（下面那张表，含 PII 字段）。写这一节时我把它当成
> 「新发现」是**表述失准** —— 发现「已知事实」时应先查项目记忆，别当新发现汇报。

本轮为了确认「其余接口有没有横向越权」，对医院侧与捕捉点侧各接口做了一次实测。
医院侧全部干净（他机构 0 条）。捕捉点侧却测出**跨机构**结果：

| 接口 | `cy_shelter`（账号区县=襄城区） | `hd_shelter`（账号区县=**全市（市级）**） |
|---|---|---|
| `/captures/` | 6 条，全本机构 ✅ | **12 条 = 本机构 3 + 他机构 9** |
| `/owner-returns/` | 3 条，全本机构 ✅ | **5 条 = 本机构 1 + 他机构 4** |
| `/releases/` | 1 条，全本机构 ✅ | **4 条 = 本机构 1 + 他机构 3** |
| `/pets/archive/` | 16 条（区县口径，设计如此） | **32 条（全市）** |

根因不在这一轮改的三处，而在 `services.get_district_filtered_queryset()`：

```python
district = getattr(user, 'district', None)
if district and getattr(district, 'is_city', False):
    return model.objects.all()          # ← 市级用户可见全部数据
```

**演示库与现场库里，4 个捕捉点账号中有 3 个的「账号区县」是「全市（市级）」**
（`hd_shelter` / `xc_shelter` / `dc_shelter`；只有 `cy_shelter` 挂在具体区县）。
于是这 3 个账号在所有走这个函数的接口上都被放大成**全市可见** ——
而 `/owner-returns/` 返回体里带 `owner_name` / `owner_phone` / `owner_id_card`。

**同一份代码对同一个事实给出了相反的处理**：

| 位置 | 对「捕捉点操作员挂在全市下」的处理 |
|---|---|
| `views_portal.hospital_pets` | **不用 `user.district`**，改用 `user.institution.district_id` 收窄（注释写明理由） |
| `services.get_district_filtered_queryset` | 直接 `return all()`，**放大到全市** |

本轮**没有动它**，因为它与第二十轮的「机构级隔离」不是同一个维度
（这是「账号区县 vs 机构区县」的读侧口径问题，第十五轮处理过写侧），
且改它会影响 4 个账号的全部列表页，属于**策略选择**：
这些操作员到底该看本机构、本机构所在区县、还是全市？
需要先确认业务意图，不宜顺手改。

#### 2.26.10 已知边界

- **机构级隔离只做了「动物 / 领养」这条线**。物料、诊疗、转运、安乐死等接口
  本轮只做了**代码级判定**（确认它们本来就有机构分支），没有逐条补横向越权测试。
- `adoption_application_list` 的捕捉点分支（`pet__shelter_id`）与
  `/pets/` 的区县口径仍不一致 —— 见 2.26.4 的说明，属「少看到」，未修。
- 区县角色（`gov_district`）在本轮未做机构级收敛：区县政府**本就该**看本区全部机构，
  这不是越权。
- 演示数据里多数接口记录太少，**单靠条数分不出「机构级」还是「区县级」**
  —— 所以本轮最终用的是**代码级判定 + 专门构造的夹具**，不是靠现有数据观察。

---

## 三、GUI 走查结论（四端）

| 端 | 走查内容 | 结论 |
|---|---|---|
| 公开门户 | 门户导航、四端卡片、公开领养大厅（匿名） | ✅ 通过；大厅空态已由种子数据修复 |
| 捕捉点端 | 登录→看板→捕捉登记全流程（编号预览、单只照片位、手写签名板、提交反馈）、转运创建（在途宠物多选、拆分下发） | ✅ 通过 |
| 医院端 | 待接收看板/待办任务联动、动物交接确认弹窗（文案明确告知状态变更）、签收成功、诊疗表单（四项说明与库存联动提示）、库存预警 | ✅ 通过 |
| 政府端 | 数据大屏（统计卡/区县统计/物料消耗图）、机构/账号/系统配置入口 | ✅ 通过 |

走查截图：`gui-test-screenshots/01~11`（门户、登录、四端工作台、捕捉表单、签名板、
转运表单、诊疗页、政府大屏、大厅空态）。

> 本节是首轮的人工走查记录。第六轮已把浏览器自动化打通（Playwright + Chromium），
> 补齐了真实渲染层的自动化实测，并**在「动物回收放养」页抓到一个此前未发现的
> 真实缺陷**（详见 2.11）。首轮记录的「部分点击事件不稳定」是当时工具链未就绪，
> 第六轮通过等待渲染完成 + 事件委托 + 直接触发业务处理函数的方式已可稳定复现。
> 第六轮截图见 `gui-test-screenshots/90~` 系列。

---

## 四、遗留建议（未在本次改动范围）

1. **菜单/图标体系**：各端菜单大量使用 emoji 图标，与水墨丹青整体风格不够统一，
   建议后续统一为 SVG 图标或调整配色（改动面大，未纳入本次保守优化）。
2. ~~**仓库瘦身**：`tnr-release-20260815.tar.gz`（510KB 发布包）、`server.log`、
   `.trae-html-share-packages/` 提交进了仓库，建议移出版本控制。~~
   **已处理**（七轮）：连同 `.mimosa/`（工具会话状态）一并 `git rm --cached`
   移出版本控制并加入 `.gitignore`。**文件仍保留在磁盘上**，只是不再被 git 跟踪；
   若要恢复跟踪，`git checkout 5e52625 -- <路径>` 即可。
3. **政府大屏性能**：一次渲染发起约 10 个 API 请求，可后续合并为聚合接口。
4. `requirements.txt` 中的 `mysqlclient` 本机构建失败（生产 MySQL 才需要），
   开发环境可从临时文件安装其余依赖；如常遇到可拆分 `requirements-dev.txt`。
5. ~~**远程未同步**：旧远程（GitHub origin）与 cnb 历史已分叉。~~
   **已处理**（第九轮结束）：一至九轮的成果共 **12 个提交**（`35dfb21` → `cec6d73`），
   连同 1 个文档更正提交（`698cfea`）共 **13 个**，已推送到**三个远程的 `main`**。
   **第十轮续推**：`652dea3`、`f2569c9` 与本次文档更正提交共 **3 个**，承接在
   `698cfea` 之后，同样推送三个远程（推送前复核「远程独有 0」，三处均可快进）：

   | 远程 | 地址 | 第九轮 | 第十轮 | 第十一轮 | 第十二轮 |
   |---|---|---|---|---|---|
   | `origin` | `https://cnb.cool/tnrxy/TNR.git` | `5e52625..cec6d73` ✔ | `698cfea..HEAD`（3 个提交）✔ | `a648700..HEAD`（4 个提交）✔ | `c0583d2..HEAD`（3 个提交）✔ |
   | `github` | `https://github.com/phdbbs/TNR.git` | `5e52625..cec6d73` ✔ | `698cfea..HEAD`（3 个提交）✔ | `a648700..HEAD`（4 个提交）✔ | `c0583d2..HEAD`（3 个提交）✔ |
   | `gitee` | `https://gitee.com/phdb/tnr.git` | `5e52625..cec6d73` ✔ | `698cfea..HEAD`（3 个提交）✔ | `a648700..HEAD`（4 个提交）✔ | `c0583d2..HEAD`（3 个提交）✔ |

   | 提交 | 内容 |
   |---|---|
   | `35dfb21` | fix: 捕捉端捕捉管理业务逻辑专项修复（第一轮） |
   | `4f7778a` | fix: 其余业务模块横向走查修复（第二轮） |
   | `b5aa49e` | fix: 种子数据与历史数据一致性（第三轮） |
   | `60e6ec9` | fix: 部署路径与安全默认值（第四轮） |
   | `a55bf98` | fix: 前后端契约一致性与区县归属（第五轮） |
   | `9ba5b38` | test: 浏览器 GUI 实测与渲染缺陷回归断言（第六轮） |
   | `c67ebca` | chore: 测试数据清理、区县名更正与测试报告（第七轮） |
   | `bf951b9` | docs: 更新遗留建议中已处理项与本地提交清单 |
   | `d462217` | fix: 区县隔离对无 `district` 字段的模型抛 FieldError（第八轮） |
   | `16af439` | feat: 捕捉端 10 项需求改造（第九轮前端） |
   | `b12f3b0` | feat: 后端配套支持 + 四端状态映射同步（第九轮后端） |
   | `cec6d73` | test: 第九轮回归测试与测试报告 |
   | `698cfea` | docs: 更正遗留建议中的推送状态与迁移编号（第九轮文档更正） |
   | `652dea3` | chore: 移除早期静态原型与冗余静态资源（第十轮） |
   | `f2569c9` | feat: 一宠一档 / 转运口径 / 看板统计 / 台账详情 / 上传校验 / 归属区县（第十轮） |
   | （本次文档更正提交） | docs: 第十轮测试报告与推送状态更正 |
   | `4141905` | feat(ui): 列表表头冻结 + 移除列表汇总标题（第十一轮） |
   | `37458b2` | chore: 忽略一次性改造脚本 `scripts/strip_mounttable_titles.py`（第十一轮） |
   | `9b1f302` | docs: 第十一轮列表冻结与去汇总标题测试报告 |
   | `c0583d2` | docs: 第十一轮推送状态回填 |
   | `a87e914` | feat(ui): 全列表补搜索条件、去列表标题、表头冻结覆盖手写表格、高度下限 10 行（第十二轮） |
   | `382a945` | test: 筛选条选项覆盖 / 取值来源 / 区县市级 三项静态回归（第十二轮） |
   | `d27cbcd` | docs: 第十二轮测试报告（搜索条件 / 分页 / 去标题 / 高度下限 10 行） |
   | （本次推送状态回填提交） | docs: 第十二轮推送状态回填 |

   推送后复核：三个远程 `main` 与本地**完全同一提交**（即 `698cfea` 之后
   连续的 3 个提交，末位为本次文档更正提交；因该提交**自引用**自己的哈希会失效，
   此处不写死末位哈希，以 `698cfea..HEAD` 表示），
   本地独有 / 远程独有均为 0。只推了 `main` 分支，未推其它分支与标签
   （GitHub 上已有 `v1.0.0` 标签，本次未动）。

   **第十一轮复核**（2026-09-18）：`origin` / `github` / `gitee` 三处
   `refs/heads/main` 经 `git ls-remote` 读出**同一哈希 `9b1f302`**，与本地 `HEAD` 一致；
   逐远程 `git rev-list --count` 双向比对，**远程独有 0 / 本地领先 0**（回填提交推送后同理）。

   **第十二轮复核**（2026-09-18）：三个提交逐一提验 —— 用 `git worktree` 检出后各跑一次
   全量测试，`a87e914` 589 通过、`382a945` 595 通过、`d27cbcd` 595 通过（均 `OK`）。
   推送后 `origin` / `github` / `gitee` 的 `refs/heads/main` 经 `git ls-remote` 读出
   **同一哈希 `d27cbcd`**，与本地 `HEAD` 一致；双向比对**远程独有 0 / 本地领先 0**
   （本次回填提交推送后同理）。本轮 `github` 未再出现代理故障 ——
   按第十一轮的结论，推送统一走 `env -u HTTP_PROXY … git -c http.proxy=`
   **直连**，三次推送一次通过。

   > **第十一轮推送踩到的坑（写下来避免再犯）**：`github` 首次推送报
   > `CONNECT tunnel failed, response 502`（代理侧临时故障，非仓库或凭据问题 ——
   > 同轮 `curl` 取 `.../info/refs` 返回 200、TCP 到 `github.com:443` 也是通的）。
   > 更值得记的是**判断方式错了**：重试循环里用
   > `grep -q "9b1f302\|Everything up-to-date"` 检测 `git push` 是否成功，
   > 而 `\|` 交替在某些 shell（如 fish）下**静默不匹配** → **推送其实已经成功，
   > 却被判成失败**，白重试 6 次并一度准备汇报「github 未推送」。
   > 最终靠 `git ls-remote` 直读哈希才纠正。
   > **结论：任何「成功/失败」判定都必须读权威源（`ls-remote` 的哈希），
   > 不要建立在文本匹配上。**

   > **第十轮拆提交的教训**：起初按「后端一批 / 前端一批」拆成 2 个提交，
   > 用 `git worktree` 逐提交验证时**两个方向都红灯**——后端先提交时
   > `core/tests_frontend_consistency.py` 报新接口「没有任何前端调用点」（死接口），
   > 前端先提交时报已下线的 `transfers/<pk>/resend/` 变成「死接口」。
   > **双向契约守卫把前后端锁在同一个提交里**，最终 `git reset --soft HEAD~2`
   > 合并为 `f2569c9`（589 用例全绿）。详见 2.15。

   > **关于拆分粒度**：第一至七轮的改动是**累积在同一个工作区**里完成的，中间状态
   > 从未提交，因此**无法按 hunk 精确还原每轮**，只能按「文件的主要改动归属哪一轮」
   > 归类，少数跨轮文件在对应提交信息里已注明。
   > **第八、九轮则做到了 hunk 级精确拆分**——两轮改动恰好落在不同 hunk，
   > 用 `git add -p` 挑选后扫描暂存区确认 0 处串味。
   >
   > **每个提交都单独验过全量测试**（`git worktree add` 检出各提交后运行）：
   > `d462217` 回归 6/6、`16af439` 491 全绿、`b12f3b0` 491 全绿、`cec6d73` 517 全绿；
   > 第十轮 `652dea3` 517 全绿、`f2569c9` 589 全绿。
   > 过程中发现并避免了一个坑：若把「四端状态映射同步」单独拆成一个后置提交，
   > `void` 会先进入后端 `choices` 而映射表未跟上，导致**中间提交处静态一致性测试变红**
   > ——已调整提交顺序，使每个提交都可用。
6. **区县 `code` 与名称语义错配**（五轮发现）：`襄城区` 的 code 是 `CY`（朝阳）、
   `樊城区` 是 `HD`（海淀）、`东津新区` 是 `XC`（西城）、`襄州区` 是 `DC`（东城），
   仍是北京缩写。判定为**非功能性**（`code` 是稳定业务键，被 `seed_data` 与迁移引用，
   仅测试断言里读到）。**用户已确认「code 不用管，未来会全部清除、全新创建」**，
   故维持现状，不做迁移。
7. **GUI 实测的可复现性**：六轮的浏览器实测脚本依赖隔离安装的 Playwright 运行时
   （`~/.workbuddy-ai/binaries/node/playwright-env`）与 Chromium 1243，
   尚未纳入仓库与 CI。如需常态化，建议把脚本与依赖清单一起入库。
8. **模板改动必须重启服务**：Django 5.0 起模板加载器恒定使用 `cached.Loader`，
   `runserver --noreload` 下改模板不会生效（六轮踩过）。开发时建议用普通
   `runserver`，或改完模板手动重启。
9. ~~**待确认**：区县名 `东经开发区` 是否为「东津开发区 / 东津新区」笔误~~
   **已处理**（七轮）：更正为「东津新区」，并新增数据迁移 `business/0012_rename_dongjing_district`
   使已部署环境可同步。详见 2.12.4。
   （迁移**有意挂在 `business` 而非 `core`**：它要改 `core.Institution.address`，
   而 `business/0011` 也会写这批地址；挂在 core 下与 `business/0011` 无依赖、
   执行顺序不确定，若先跑则笔误地址会被重新写回。详见 2.12.4。）

10. **区县/机构等基础数据将重建**：用户已明确区县 `code` 与部分基础数据未来会
    **全部清除、全新创建**。届时 `seed_data` 的区县段与 `business/0010`、
    `business/0011`、`business/0012` 这三个「改名类」数据迁移的历史守卫会自然失效
    （无匹配行则空跑），无需额外清理，但重建后建议重跑一次 `manage.py test`
    确认测试断言仍然成立。

11. **⚠️ 待定夺（第二十轮顺带发现）：捕捉点操作员挂在「全市（市级）」下，
    读范围被放大成全市** —— 详见 2.26.9。

    - **现象**：4 个捕捉点账号里 3 个（`hd_shelter` / `xc_shelter` / `dc_shelter`）
      的**账号区县**是「全市（市级）」，而 `get_district_filtered_queryset()` 对
      市级用户直接 `return all()`。实测 `hd_shelter`：捕捉单 12 条（本机构 3 + 他机构 9）、
      主人领回 5 条（1 + 4）、放养 4 条（1 + 3）、档案 32 条（全市）。
      `/owner-returns/` 的返回体含 `owner_name` / `owner_phone` / `owner_id_card`。
      （「账号挂全市」这个**事实**在 `MEMORY-deploy.md` 早有记录；
      **新的是**它与 `hospital_pets` 的处理相反、以及上面这份量化影响面。）
    - **为什么说是「不一致」而不是「设计如此」**：`views_portal.hospital_pets`
      对**同一个事实**给出的处理是相反的 —— 它**特意不用 `user.district`**，
      改用 `user.institution.district_id`，注释写明「现场把捕捉点操作员都挂在
      「全市（市级）」下，按 `user.district` 会返回全市宠物」。
      两处对同一批账号，一处收窄、一处放大。
    - **为什么本轮不改**：与第二十轮的「机构级隔离」不是同一维度（这是
      「账号区县 vs 机构区县」的**读侧**口径，第十五轮只处理了写侧）；
      且会影响这 3 个账号的全部列表页。**先要确认业务意图**：
      这些操作员该看「本机构」「本机构所在区县」还是「全市」？
    - **候选修法**（择一，需确认后实施）：
      1. 把 `get_district_filtered_queryset` 的市级分支改成「按 `user.institution.district`
         收敛」（与 `hospital_pets` 对齐）—— 改动面最大但最一致；
      2. 只改 `shelter` 角色：新增 `shelter_district_scope(user)` 判据，在捕捉点端的
         列表接口上替换掉 `get_district_filtered_queryset`；
      3. 不动代码，改为**校正数据**：把这 3 个账号的区县改成其机构所在区县
         （第十五轮的 `check_data_integrity` 已有「账号区县 ↔ 机构区县」这一项，
         但显然**没有把「账号挂市级」判为不一致**，若选这条需同步收紧该检查）。

---

## 五、复测指南

```bash
cd /Users/wl/M4/TNR
source .venv/bin/activate
python manage.py migrate
python manage.py seed_data          # 幂等，可重复执行；同时校准演示账号
python manage.py check --deploy     # 生产部署前自检
python manage.py check_data_integrity   # 数据一致性巡检（只读，有违规退出码 1）
python manage.py test --parallel 1  # 769 个用例
python manage.py runserver          # http://127.0.0.1:8000
# 演示账号（密码统一 123456）：admin / cy_shelter / babitang_hosp / adopter1
# 9 个演示账号均可用（含 hd_shelter、aixin_hosp），详见 DEMO_ACCOUNTS.md
# 注意：生产环境经 deploy.sh 部署后，admin 口令会被超级管理员口令覆盖
```

GUI 实测脚本（`gui-test-scripts/`，需先起 `runserver`）：

```bash
NODE_PATH=/Users/wl/.workbuddy-ai/binaries/node/playwright-env/node_modules \
/Users/wl/.workbuddy-ai/binaries/node/versions/22.22.2-2/bin/node gui-test-scripts/<脚本>
# 46 全端 80 视图冒烟｜47 筛选条守卫｜55 操作日志｜56 账号区县一致性
# 57 机构表单区县规则（区县下拉收敛 / 绕过界面在他区建机构 / 挂市级机构不被静默改判）
# 58 前端收窄选项（区县级别守卫 / 停用区县 / 捕捉点↔归属区县一致性，20 项）
# 59 停用状态生效（捕捉点端机构下拉 / 区县下拉 / 四处接口拦截，9 项）
# 60 编号规则配置读/写权限（区级只读 / 市级可改 / 绕过界面写被拒，16 项）
# 61 医院横向越权收口（两家同区县医院可见集合不相交 / 档案与溯源机构级，19 项）
# 注：56 与 58/59 自带开场+收尾清理（政府端用户/机构没有删除入口，只能从库清）
```
